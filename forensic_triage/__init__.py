"""forensic_triage — accounting-forensics screen over the coverage universe.

Package layout per workspace CONVENTIONS.md §3b (repackaged 2026-07-23 from
the original flat root layout): the single root launcher `run_unattended.py`
shims into this package; every other module runs as
`python -m forensic_triage.<module>` from the project root.
"""
