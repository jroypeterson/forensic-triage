# Codex feedback — 2026-07-13_211218

Auto-generated by root `scripts/codex_review.sh`. The Codex banner below (version, model,
session id) is emitted by Codex itself — that is the verification it actually ran.

- generated_at: 2026-07-13T21:12:18-04:00
- codex_cli: codex-cli 0.141.0
- mode: plan
- reasoning_effort: xhigh
- cwd: /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
- git_head: 6fd3849

-----------------------------------------------------------------------

Reading additional input from stdin...
OpenAI Codex v0.141.0
--------
workdir: /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
model: gpt-5.5
provider: openai
approval: never
sandbox: read-only
reasoning effort: xhigh
reasoning summaries: none
session id: 019f5e2e-9654-77a1-9e4e-4627c9097fe4
--------
user
This is a round-2 VERIFICATION review. In round 1 you found 1 Critical + 3 High issues in the `forensic_triage` coverage-dashboard / S&P-500-expansion work. Below is the fix diff. For EACH of the four findings, confirm whether the fix is correct and complete, or whether it's incomplete/wrong. Then hunt for any NEW Critical/High bug the fixes themselves introduced.

The four round-1 findings and the intended fixes:
1. CRITICAL — `run_unattended._pick_batch` called `next_batch.sort_key()` without the required `rosters` arg → TypeError. Fix: pass `rosters = nb.cc.load_rosters()` into a `lambda r: nb.sort_key(r, rosters)`, and default `sort_key(row, rosters=None)` so it can't crash again (relies on the baked `cohort` column / `cohort_for` live-load when rosters is None/empty).
2. HIGH — a transient SEC ticker-map outage dropped existing S&P-500 rows. Fix: in `build_sp500_extras`, fall back to `prev["cik"]` (the existing row's CIK) when SEC resolution returns nothing, so an outage can't drop established rows.
3. HIGH — class-ticker punctuation (BRK.B vs BRK-B) could double-add a name. Fix: new `ckey()` folds `.`→`-`; `cm_master`/`already_added` compared on `ckey`.
4. HIGH — sync silently baked empty/poisoned cohorts when rosters were missing. Fix: sync now returns rc=1 (aborts before writing) if `coverage_cohorts` import fails or any of portfolio/core/sp500 rosters are empty.

Check specifically:
- Does the `run_unattended` fix actually order correctly in CI where `load_rosters()` returns empty sets? (The baked `cohort` column should carry it — confirm `sort_key`→`row_cohort` reads the baked column before falling back.)
- Does the abort-on-empty-rosters guard have a false-positive risk (e.g. a legitimately empty `researching` roster)? It only checks portfolio/core/sp500 — is that right?
- Does the prev-CIK fallback ever resurrect a STALE/wrong CIK, or mask a real removal?
- Any ordering bug: is `prev` now referenced before assignment anywhere?
- Do the new regression tests actually assert the right behavior?

Report ONLY Critical/High. For each round-1 finding say "FIXED" / "PARTIAL" / "NOT FIXED" with a one-line reason. If no new Critical/High issues, say "No new Critical/High issues found." Skip nits.

=== FIX DIFF ===
diff --git a/next_batch.py b/next_batch.py
index e05a132..0d44eeb 100644
--- a/next_batch.py
+++ b/next_batch.py
@@ -65,13 +65,15 @@ def screened_since(cycle_start: str) -> set[str]:
     return done
 
 
-def row_cohort(row: dict, rosters: dict[str, set[str]]) -> str:
-    """Baked `cohort` column (CI-safe) if present, else compute live from rosters."""
+def row_cohort(row: dict, rosters: dict[str, set[str]] | None = None) -> str:
+    """Baked `cohort` column (CI-safe) if present, else compute live from rosters.
+    `rosters=None` → `cohort_for` loads them live (defensive; avoids a hard dep on
+    the caller pre-loading rosters)."""
     c = (row.get("cohort") or "").strip()
     return c if c else cc.cohort_for(row.get("ticker", ""), rosters)
 
 
-def sort_key(row: dict, rosters: dict[str, set[str]]):
+def sort_key(row: dict, rosters: dict[str, set[str]] | None = None):
     cohort_rank = cc.COHORT_RANK.get(row_cohort(row, rosters), 99)
     sg_rank = SUBGROUP_ORDER.get(row.get("sector_subgroup", ""), 9)
     return (cohort_rank, sg_rank, row.get("ticker", ""))
diff --git a/run_unattended.py b/run_unattended.py
index 0cd6f4d..3d61674 100644
--- a/run_unattended.py
+++ b/run_unattended.py
@@ -56,10 +56,11 @@ def _pick_batch(n: int, cycle_start: str) -> list[str]:
     """Next n un-screened domestic names (reuse next_batch's selection logic)."""
     import next_batch as nb
     watch = nb.load_watchlist()
+    rosters = nb.cc.load_rosters()  # empty in CI; sort_key falls back to the baked `cohort` column
     domestic = [r for r in watch if r.get("filer_type", "domestic") != "foreign"]
     done = nb.screened_since(cycle_start)
     pending = [r for r in domestic if r["ticker"].strip() not in done]
-    pending.sort(key=nb.sort_key)
+    pending.sort(key=lambda r: nb.sort_key(r, rosters))
     return [r["ticker"].strip().upper() for r in pending[:n]]
 
 
diff --git a/sync_watchlist.py b/sync_watchlist.py
index d874ca3..2c1b575 100644
--- a/sync_watchlist.py
+++ b/sync_watchlist.py
@@ -85,6 +85,14 @@ WATCHLIST_FIELDS = [
 COHORT_TOTALS_JSON = ROOT / "data" / "cohort_totals.json"
 
 
+def ckey(t: str) -> str:
+    """Canonical ticker key for cross-source comparison. SEC / sigma-S&P / CM disagree
+    on class-share punctuation (`BRK.B` vs `BRK-B`, `BF.B` vs `BF-B`); fold `.`→`-` so
+    the same security matches across sources and isn't double-added. Distinct tickers
+    like GOOG vs GOOGL are unaffected (different letters, not punctuation)."""
+    return (t or "").strip().upper().replace(".", "-")
+
+
 def normalize_cik(raw: str) -> str:
     """Normalize a Coverage Manager CIK to a zero-padded 10-digit string.
 
@@ -258,19 +266,27 @@ def build_sp500_extras(
     # (Using just the sp500 roster avoids pulling in foreign CM-position tickers whose
     # export format differs from the CM master, which have no SEC CIK anyway.)
     core_set = rosters["core"]
+    # Compare on the canonical key so a class-ticker CM already carries (BRK-B) isn't
+    # re-added under sigma's spelling (BRK.B).
+    cm_canon = {ckey(t) for t in cm_master}
+    added_canon = {ckey(t) for t in already_added}
     extras = sorted(t for t in rosters["sp500"]
-                    if t not in cm_master and t not in already_added)
+                    if ckey(t) not in cm_canon and ckey(t) not in added_canon)
 
     sec_ciks = load_sec_ticker_ciks() if extras else {}
     rows: list[dict] = []
     added: list[str] = []
     unresolved: list[str] = []
     for ticker in extras:
-        cik = sec_ciks.get(ticker) or sec_ciks.get(ticker.replace(".", "-"))
+        prev = existing.get(ticker)
+        # Prefer a fresh SEC resolution, but fall back to the already-known CIK on an
+        # existing row so a transient SEC-map outage can't DROP established S&P rows
+        # (which would then be reported "removed" and written out of the watchlist).
+        cik = (sec_ciks.get(ticker) or sec_ciks.get(ticker.replace(".", "-"))
+               or (prev.get("cik") if prev and prev.get("cik") else ""))
         if not cik:
             unresolved.append(ticker)
             continue  # no CIK -> can't EDGAR-screen; skip loudly (reported below)
-        prev = existing.get(ticker)
         rows.append({
             "ticker": ticker,
             "company_name": prev.get("company_name", "") if prev else "",
@@ -340,23 +356,33 @@ def main() -> int:
 
     # Bake the cohort onto every row (+ disjoint roster totals) so CI — which has no
     # CM/sigma sibling repos — reads cohorts from the committed watchlist, not live.
+    # The rosters are REQUIRED for a correct local sync: if the sibling exports are
+    # missing/empty, cohort_for would stamp everything "other"/"sp500" and bake zeros
+    # into cohort_totals.json — poisoned columns CI then trusts. So abort the write
+    # rather than persist a mis-partitioned universe.
     cohort_totals: dict[str, int] = {}
     try:
         import coverage_cohorts as cc
         rosters = cc.load_rosters()
-        for r in new_rows:
-            r["cohort"] = cc.cohort_for(r["ticker"], rosters)
-        p, rr, co, sp = (rosters["portfolio"], rosters["researching"],
-                         rosters["core"], rosters["sp500"])
-        cohort_totals = {
-            "portfolio": len(p), "researching": len(rr - p),
-            "core": len(co - rr - p), "sp500": len(sp - co - rr - p),
-        }
     except Exception as e:
-        print(f"WARNING: cohort stamping failed ({e}); rows left without a cohort.",
-              file=sys.stderr)
-        for r in new_rows:
-            r.setdefault("cohort", "")
+        print(f"ERROR: coverage_cohorts unavailable ({e}); cannot bake cohorts — refusing "
+              f"to write a watchlist without them.", file=sys.stderr)
+        return 1
+    missing = [c for c in ("portfolio", "core", "sp500") if not rosters.get(c)]
+    if missing:
+        print(f"ERROR: coverage rosters empty/missing: {', '.join(missing)}. The CM exports "
+              f"(portfolio.json/universe_metadata.json) + sigma-alert/sources/sp500.txt are "
+              f"REQUIRED for a correct local sync. Refusing to bake poisoned cohorts into "
+              f"watchlist.csv — check the sibling repos.", file=sys.stderr)
+        return 1
+    for r in new_rows:
+        r["cohort"] = cc.cohort_for(r["ticker"], rosters)
+    p, rr, co, sp = (rosters["portfolio"], rosters["researching"],
+                     rosters["core"], rosters["sp500"])
+    cohort_totals = {
+        "portfolio": len(p), "researching": len(rr - p),
+        "core": len(co - rr - p), "sp500": len(sp - co - rr - p),
+    }
 
     by_subgroup: dict[str, int] = {}
     for r in new_rows:
diff --git a/tests/test_sync_cohorts.py b/tests/test_sync_cohorts.py
new file mode 100644
index 0000000..15756c3
--- /dev/null
+++ b/tests/test_sync_cohorts.py
@@ -0,0 +1,63 @@
+"""Regression tests for the S&P 500 sync + cohort wiring (Codex round-1 findings)."""
+from __future__ import annotations
+
+import sys
+from pathlib import Path
+
+sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
+
+import coverage_cohorts as cc  # noqa: E402
+import next_batch as nb  # noqa: E402
+import sync_watchlist as sw  # noqa: E402
+
+
+def test_ckey_folds_class_punctuation():
+    # BRK.B / BRK-B are the same security across sources; GOOG vs GOOGL are not.
+    assert sw.ckey("BRK.B") == "BRK-B"
+    assert sw.ckey("brk.b") == "BRK-B"
+    assert sw.ckey("GOOGL") == "GOOGL"
+    assert sw.ckey("GOOG") != sw.ckey("GOOGL")
+
+
+def test_sort_key_defensive_without_rosters():
+    # Codex Critical: run_unattended called sort_key() with no rosters -> TypeError.
+    # sort_key must be callable without rosters (uses the baked cohort column).
+    k = nb.sort_key({"ticker": "AAA", "cohort": "portfolio"})
+    assert k[0] == cc.COHORT_RANK["portfolio"]
+    k2 = nb.sort_key({"ticker": "ZZZ", "cohort": "sp500"})
+    assert k2[0] == cc.COHORT_RANK["sp500"]
+    assert k < k2  # portfolio sorts before sp500
+
+
+def test_sp500_extras_preserves_cik_on_sec_failure(monkeypatch):
+    # Codex High: a transient SEC-map outage must NOT drop an existing S&P row
+    # (which would then be reported "removed" and written out of the watchlist).
+    monkeypatch.setattr(cc, "load_rosters", lambda: {
+        "portfolio": set(), "researching": set(), "core": set(), "sp500": {"ABNB"}})
+    monkeypatch.setattr(sw, "load_sec_ticker_ciks", lambda: {})  # SEC map unavailable
+    existing = {"ABNB": {"cik": "0001559720", "added_date": "2026-01-01",
+                         "company_name": "Airbnb"}}
+    rows, added, unresolved = sw.build_sp500_extras(
+        existing, cm_master=set(), already_added=set(), today="2026-07-13")
+    assert unresolved == []                       # NOT dropped
+    assert len(rows) == 1 and rows[0]["cik"] == "0001559720"
+
+
+def test_sp500_extras_dedup_class_ticker(monkeypatch):
+    # Codex High: CM carrying BRK-B must suppress re-adding sigma's BRK.B spelling.
+    monkeypatch.setattr(cc, "load_rosters", lambda: {
+        "portfolio": set(), "researching": set(), "core": set(), "sp500": {"BRK.B"}})
+    monkeypatch.setattr(sw, "load_sec_ticker_ciks", lambda: {"BRK-B": "0001067983"})
+    rows, added, unresolved = sw.build_sp500_extras(
+        {}, cm_master={"BRK-B"}, already_added=set(), today="2026-07-13")
+    assert rows == [] and added == []             # no duplicate row
+
+
+def test_sp500_extras_new_name_without_cik_is_skipped(monkeypatch):
+    # A brand-new S&P name with no prev row and no SEC CIK is skipped loudly (can't screen).
+    monkeypatch.setattr(cc, "load_rosters", lambda: {
+        "portfolio": set(), "researching": set(), "core": set(), "sp500": {"NEWCO"}})
+    monkeypatch.setattr(sw, "load_sec_ticker_ciks", lambda: {})
+    rows, added, unresolved = sw.build_sp500_extras(
+        {}, cm_master=set(), already_added=set(), today="2026-07-13")
+    assert rows == [] and unresolved == ["NEWCO"]
codex
I’ll verify against the actual files rather than just the pasted diff, then check the affected call paths and tests for any Critical/High regressions.
exec
/bin/bash -lc "sed -n '1,130p' run_unattended.py" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
"""Per-run orchestrator for the unattended (Path A) forensic screen.

Ties the pipeline together (PATH_A_PLAN step 5):
  next_batch  ->  edgar_fetch (per ticker)  ->  tier_batch (Anthropic judge + guardrails)
              ->  append flags_history       ->  write a compact report
              ->  (separately) notify Slack.

The git commit/push happens in the WORKFLOW between the screen step and the notify step (so the
commit hash is known); this script writes a small `data/last_run.json` the notify step reads for
the commit + results. The script NEVER spends API budget when run with --notify-only / --failure-alarm.

Modes:
  (default)         run the screen: fetch + tier + history + report, write last_run.json
  --notify-only     read last_run.json + post #forensic-flags result + #status-reports heartbeat
  --failure-alarm   post a loud failure alarm to #status-reports (the if: failure() path)

Run-level circuit breaker: if the batch can't be evaluated (broad outage), exit non-zero so the
workflow's `if: failure()` alarm fires and NO batch of false Data Gaps is committed.
"""
from __future__ import annotations

import argparse
import json
import os
import subprocess
import sys
from datetime import date
from pathlib import Path

ROOT = Path(__file__).parent
sys.path.insert(0, str(ROOT))

import edgar_fetch  # noqa: E402
import notify  # noqa: E402
import tier_batch  # noqa: E402
from forensic_schema import FAMILIES  # noqa: E402

DATA = ROOT / "data"
REPORTS = ROOT / "reports"
LAST_RUN = DATA / "last_run.json"
WATCHLIST = DATA / "watchlist.csv"
DEFAULT_CYCLE_START = "2026-06-20"


def _load_watchlist() -> dict[str, dict]:
    import csv
    out = {}
    if WATCHLIST.exists():
        with WATCHLIST.open(encoding="utf-8", newline="") as f:
            for row in csv.DictReader(f):
                out[(row.get("ticker") or "").upper()] = row
    return out


def _pick_batch(n: int, cycle_start: str) -> list[str]:
    """Next n un-screened domestic names (reuse next_batch's selection logic)."""
    import next_batch as nb
    watch = nb.load_watchlist()
    rosters = nb.cc.load_rosters()  # empty in CI; sort_key falls back to the baked `cohort` column
    domestic = [r for r in watch if r.get("filer_type", "domestic") != "foreign"]
    done = nb.screened_since(cycle_start)
    pending = [r for r in domestic if r["ticker"].strip() not in done]
    pending.sort(key=lambda r: nb.sort_key(r, rosters))
    return [r["ticker"].strip().upper() for r in pending[:n]]


def run_screen(*, batch_size: int, run_id: str, cycle_start: str) -> int:
    wl = _load_watchlist()
    tickers = _pick_batch(batch_size, cycle_start)
    if not tickers:
        print("Cycle complete — no pending domestic names. Nothing to screen.")
        _write_last_run(run_id=run_id, results=[], note="cycle complete (no pending names)")
        return 0

    print(f"Run {run_id}: screening {len(tickers)} names: {', '.join(tickers)}")
    new_set = tier_batch._new_names()

    results = []
    for ticker in tickers:
        row = wl.get(ticker, {})
        cik = (row.get("cik") or "").strip()
        subgroup = row.get("sector_subgroup") or "general"
        filer_type = row.get("filer_type") or "domestic"

        # 1. fetch (never raises)
        rec = edgar_fetch.fetch_ticker(
            ticker, cik, subgroup=subgroup, filer_type=filer_type, run_id=run_id,
        )
        edgar_fetch.write_record(rec, edgar_fetch.DEFAULT_OUT_DIR)

        # 2. tier (Anthropic judge + deterministic guardrails)
        res = tier_batch.tier_one(rec, subgroup=subgroup, is_new=(ticker in new_set))
        results.append(res)
        print(f"  {ticker:<6} {res['tier']:<16} status={res['status']}  {res['reason']}")

    # 3. run-level circuit breaker (broad outage -> fail loudly, commit nothing)
    tripped, why = tier_batch.circuit_breaker_tripped(results)
    if tripped:
        print(f"\nCIRCUIT BREAKER TRIPPED: {why}")
        _write_last_run(run_id=run_id, results=results, note=f"CIRCUIT BREAKER: {why}", ok=False)
        # Non-zero exit so the workflow alarms and skips the commit.
        return 2

    # 4. append history (only complete + fetch_failed rows; fetch_failed retries next run)
    rows = [tier_batch.result_to_history_row(r, run_id=run_id) for r in results]
    tier_batch.append_history(rows)

    # 5. compact report
    report_path = _write_report(results, run_id=run_id)
    print(f"\nWrote report {report_path}; appended {len(rows)} history rows.")

    _write_last_run(run_id=run_id, results=results, note="ok", ok=True)
    return 0


def _counts(results: list[dict]) -> dict:
    c: dict[str, int] = {}
    for r in results:
        c[r["tier"]] = c.get(r["tier"], 0) + 1
    return c


def _missing_required(results: list[dict]) -> int:
    n = 0
    for r in results:
        cov = r.get("coverage", {})
        if any(cov.get(f) not in ("complete", "not_applicable", None) for f in FAMILIES):
            # any non-complete required-ish family -> count as a name with a coverage gap
            from forensic_schema import required_families
            sg = r.get("subgroup", "general")

exec
/bin/bash -lc "sed -n '1,220p' coverage_cohorts.py" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
"""Coverage cohorts — partition the screenable universe into JP's priority rings.

The forensic coverage dashboard (and the screening priority order) group every
name into ONE of four disjoint rings, widest-priority first:

    portfolio  ->  researching  ->  core  ->  sp500   (+ `other` residual)

A name counts under its HIGHEST-priority ring only (a Portfolio name that is also
in the S&P 500 is `portfolio`, never `sp500`). This mirrors the tier partition in
``transcripts/coverage.py`` (`_tiers_partition`) so the two projects read the same
Coverage-Manager exports the same way.

Rosters read (all free, local files):
  - portfolio    <- Coverage Manager/exports/portfolio.json      (keys = tickers)
  - researching  <- Coverage Manager/exports/researching.json
  - core         <- Coverage Manager/exports/universe_metadata.json  (core == "Y")
  - sp500        <- sigma-alert/sources/sp500.txt                 (one ticker/line)

`other` = a name in forensic's watchlist that is in none of the four rings (a CM
coverage name that is not Portfolio/Researching/Core and not in the S&P 500). It's
surfaced, never hidden — silent truncation is the failure mode we avoid.
"""
from __future__ import annotations

import json
from pathlib import Path

ROOT = Path(__file__).parent
CM_EXPORTS = ROOT.parent / "Coverage Manager" / "exports"
SP500_FILE = ROOT.parent / "sigma-alert" / "sources" / "sp500.txt"

# Priority order, widest ring last. `other` is the residual bucket.
COHORT_ORDER = ["portfolio", "researching", "core", "sp500", "other"]
COHORT_RANK = {c: i for i, c in enumerate(COHORT_ORDER)}
COHORT_LABEL = {
    "portfolio": "Portfolio",
    "researching": "Researching",
    "core": "Core coverage",
    "sp500": "S&P 500",
    "other": "Other coverage",
}


def _norm(t: str) -> str:
    return (t or "").strip().upper()


def _load_json_keys(fname: str) -> set[str]:
    p = CM_EXPORTS / fname
    if not p.exists():
        return set()
    try:
        d = json.loads(p.read_text(encoding="utf-8"))
        return {_norm(k) for k in d.keys() if k}
    except Exception:
        return set()


def load_portfolio() -> set[str]:
    return _load_json_keys("portfolio.json")


def load_researching() -> set[str]:
    return _load_json_keys("researching.json")


def load_core() -> set[str]:
    """Core = universe_metadata.json entries flagged core == 'Y'."""
    p = CM_EXPORTS / "universe_metadata.json"
    if not p.exists():
        return set()
    try:
        meta = json.loads(p.read_text(encoding="utf-8"))
        return {_norm(t) for t, v in meta.items() if (v or {}).get("core") == "Y"}
    except Exception:
        return set()


def load_sp500() -> set[str]:
    """S&P 500 tickers from sigma-alert's Wikipedia-scrape source file."""
    if not SP500_FILE.exists():
        return set()
    out: set[str] = set()
    for line in SP500_FILE.read_text(encoding="utf-8").splitlines():
        line = line.strip()
        if not line or line.startswith("#"):
            continue
        out.add(_norm(line))
    return out


def load_rosters() -> dict[str, set[str]]:
    """All four rosters as RAW (non-disjoint) sets, keyed by cohort name."""
    return {
        "portfolio": load_portfolio(),
        "researching": load_researching(),
        "core": load_core(),
        "sp500": load_sp500(),
    }


def cohort_for(ticker: str, rosters: dict[str, set[str]] | None = None) -> str:
    """Return the ticker's cohort by priority (portfolio>researching>core>sp500>other)."""
    if rosters is None:
        rosters = load_rosters()
    t = _norm(ticker)
    for c in ("portfolio", "researching", "core", "sp500"):
        if t in rosters.get(c, set()):
            return c
    return "other"


def assign_cohorts(tickers: list[str] | set[str],
                   rosters: dict[str, set[str]] | None = None) -> dict[str, str]:
    """Map each ticker -> its disjoint cohort. Every input ticker lands somewhere."""
    if rosters is None:
        rosters = load_rosters()
    return {_norm(t): cohort_for(t, rosters) for t in tickers}


def sort_key(cohort: str, ticker: str) -> tuple:
    """Screening priority: portfolio -> researching -> core -> sp500 -> other, then A-Z."""
    return (COHORT_RANK.get(cohort, 99), _norm(ticker))

exec
/bin/bash -lc "sed -n '1,180p' next_batch.py" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
"""Pick the next batch of names to screen — the daily "a few each day" driver.

Path B (interactive) chips through the domestic watchlist names a few at a
time. This deterministic helper picks the next batch so each session knows exactly
what to screen, prioritised by JP's coverage rings:

  1. by cohort: portfolio -> researching -> core -> sp500 -> other
  2. then by subgroup within a cohort: hc_services -> medtech -> general (HC focus)
  3. then alphabetical

A name is "done this cycle" once it has a flags_history row dated >= CYCLE_START
(the recalibration baseline) WITH status=complete (codex R2 idempotency). A row
written by a transient fetch failure (status=fetch_failed) does NOT mark the name
done — it retries next run. A structural Data Gap (foreign/stale/not-disclosed) is
written status=complete and IS done. Older rows without a status column are treated
as complete (back-compat). Foreign filers (filer_type=foreign) are never screened by
EDGAR — they belong to the Data Gap tier — so they're excluded here and reported separately.

Usage:
  python next_batch.py            # show the next batch (default 8) + progress
  python next_batch.py -n 5       # batch of 5
  python next_batch.py --cycle-start 2026-06-20
"""
from __future__ import annotations

import argparse
import csv
from pathlib import Path

import coverage_cohorts as cc

ROOT = Path(__file__).parent
WATCHLIST_CSV = ROOT / "data" / "watchlist.csv"
FLAGS_HISTORY_CSV = ROOT / "data" / "flags_history.csv"

# Names screened on/after this date count as done for the current cycle. Bump it
# when a fresh full re-screen cycle starts. Default = the 2026-06-20 recalibration.
DEFAULT_CYCLE_START = "2026-06-20"

SUBGROUP_ORDER = {"hc_services": 0, "medtech": 1, "general": 2}


def load_watchlist() -> list[dict]:
    with WATCHLIST_CSV.open(encoding="utf-8", newline="") as f:
        return list(csv.DictReader(f))


def screened_since(cycle_start: str) -> set[str]:
    """Tickers DONE this cycle: a flags_history row dated >= cycle_start with status=complete.

    A `status=fetch_failed` row does NOT count as done (transient failure retries next run).
    Rows without a `status` column (legacy interactive runs) are treated as complete."""
    if not FLAGS_HISTORY_CSV.exists():
        return set()
    done: set[str] = set()
    with FLAGS_HISTORY_CSV.open(encoding="utf-8", newline="") as f:
        reader = csv.DictReader(f)
        has_status = "status" in (reader.fieldnames or [])
        for row in reader:
            if (row.get("run_date") or "") < cycle_start or not row.get("ticker"):
                continue
            status = (row.get("status") or "").strip() if has_status else "complete"
            if status in ("", "complete"):
                done.add(row["ticker"].strip())
    return done


def row_cohort(row: dict, rosters: dict[str, set[str]] | None = None) -> str:
    """Baked `cohort` column (CI-safe) if present, else compute live from rosters.
    `rosters=None` → `cohort_for` loads them live (defensive; avoids a hard dep on
    the caller pre-loading rosters)."""
    c = (row.get("cohort") or "").strip()
    return c if c else cc.cohort_for(row.get("ticker", ""), rosters)


def sort_key(row: dict, rosters: dict[str, set[str]] | None = None):
    cohort_rank = cc.COHORT_RANK.get(row_cohort(row, rosters), 99)
    sg_rank = SUBGROUP_ORDER.get(row.get("sector_subgroup", ""), 9)
    return (cohort_rank, sg_rank, row.get("ticker", ""))


def main() -> int:
    p = argparse.ArgumentParser(description=__doc__)
    p.add_argument("-n", "--batch-size", type=int, default=8, help="How many names this batch")
    p.add_argument("--cycle-start", default=DEFAULT_CYCLE_START,
                   help=f"Names screened on/after this date count as done (default {DEFAULT_CYCLE_START})")
    args = p.parse_args()

    watchlist = load_watchlist()
    rosters = cc.load_rosters()
    domestic = [r for r in watchlist if r.get("filer_type", "domestic") != "foreign"]
    foreign = [r for r in watchlist if r.get("filer_type") == "foreign"]
    done = screened_since(args.cycle_start)

    pending = [r for r in domestic if r["ticker"].strip() not in done]
    pending.sort(key=lambda r: sort_key(r, rosters))

    n_dom = len(domestic)
    n_done = sum(1 for r in domestic if r["ticker"].strip() in done)
    print(f"Cycle start: {args.cycle_start}")
    print(f"Domestic screened this cycle: {n_done}/{n_dom}  ({n_dom - n_done} pending)")
    print(f"Foreign (Data Gap, not EDGAR-screened): {len(foreign)}")
    print()
    if not pending:
        print("Cycle complete - every domestic name screened since cycle start. Bump --cycle-start to re-screen.")
        return 0

    batch = pending[:args.batch_size]
    print(f"Next batch ({len(batch)}):")
    for r in batch:
        cohort = row_cohort(r, rosters)
        print(f"  {r['ticker']:<6} [{cohort:<11}] {r['sector_subgroup']:<12} {r.get('company_name','')}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())

exec
/bin/bash -lc "sed -n '1,430p' sync_watchlist.py" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
"""Sync forensic_triage/data/watchlist.csv from Coverage Manager master list.

Reads:  ../Coverage Manager/data/coverage_universe_tickers.csv
Writes: data/watchlist.csv

Rules:
  - Only US-listed names (must have a CIK — EDGAR is the data source)
  - Exclude Biopharma (forensic triage de-prioritizes biotech accounting)
  - Map Sector (JP) -> forensic subgroup (see SUBGROUP_MAP)
  - Preserve manual `notes` and original `added_date` for tickers that survive
  - Report adds / removes / subgroup changes to stdout

Run:  python sync_watchlist.py
      python sync_watchlist.py --dry-run
"""

from __future__ import annotations

import argparse
import csv
import json
import os
import sys
import urllib.request
from datetime import date
from pathlib import Path

ROOT = Path(__file__).parent
WATCHLIST_CSV = ROOT / "data" / "watchlist.csv"

# SEC ticker->CIK map, used to resolve CIKs for the S&P 500 names that expand the
# universe beyond CM coverage (the 4th coverage ring). Free, cached locally.
SEC_TICKERS_URL = "https://www.sec.gov/files/company_tickers.json"
SEC_TICKERS_CACHE = ROOT / "data" / ".sec_company_tickers.json"

# Coverage CSV path resolution:
#   1. FORENSIC_COVERAGE_CSV env var (used by remote scheduled trigger)
#   2. Local sibling layout: ../Coverage Manager/data/coverage_universe_tickers.csv
def resolve_coverage_csv() -> Path:
    env_path = os.environ.get("FORENSIC_COVERAGE_CSV")
    if env_path:
        return Path(env_path)
    return ROOT.parent / "Coverage Manager" / "data" / "coverage_universe_tickers.csv"

# Map Coverage Manager Sector (JP) -> forensic_triage sector_subgroup.
# Kept in sync with Coverage Manager's ALLOWED_SECTORS_JP (config.py).
# Coverage Manager retired "PA" and "Healthcare Real Estate" on 2026-04-17
# (collapsed into "Other" and "Healthcare Services" respectively). The new
# "Healthcare Real Estate" subsector rolls up under hc_services here.
SUBGROUP_MAP = {
    "Healthcare Services": "hc_services",
    "MedTech": "medtech",
    "Life Science Tools": "medtech",
    # Large-cap biopharma (S&P 500 only — see LARGE_CAP_ONLY_SECTORS) uses the general
    # rubric for now (no biopharma-specific rubric yet — a future addition).
    "Biopharma": "general",
    # Everything else that survives the filter falls through to "general"
    "Tech": "general",
    "SaaS": "general",
    "Fintech": "general",
    "Other": "general",
}

EXCLUDED_SECTORS: set[str] = set()  # (was {"Biopharma"} — now conditional, see below)

# Biopharma is screened ONLY when large-cap (proxied by S&P 500 membership): big pharma
# (LLY/PFE/MRK…) has normal financials the rubric handles; small/pre-revenue biotech
# accruals/revenue rules are noise, so non-S&P-500 biopharma stays excluded. (JP 2026-07-13.)
LARGE_CAP_ONLY_SECTORS = {"Biopharma"}

WATCHLIST_FIELDS = [
    "ticker",
    "company_name",
    "cik",
    "sector_subgroup",
    "subsector",
    "core",
    "filer_type",
    "added_date",
    "notes",
    "source",  # "cm" (Coverage Manager universe) or "sp500" (S&P 500 expansion ring)
    "cohort",  # portfolio|researching|core|sp500|other — baked so CI (no sibling repos) reads it
]

COHORT_TOTALS_JSON = ROOT / "data" / "cohort_totals.json"


def ckey(t: str) -> str:
    """Canonical ticker key for cross-source comparison. SEC / sigma-S&P / CM disagree
    on class-share punctuation (`BRK.B` vs `BRK-B`, `BF.B` vs `BF-B`); fold `.`→`-` so
    the same security matches across sources and isn't double-added. Distinct tickers
    like GOOG vs GOOGL are unaffected (different letters, not punctuation)."""
    return (t or "").strip().upper().replace(".", "-")


def normalize_cik(raw: str) -> str:
    """Normalize a Coverage Manager CIK to a zero-padded 10-digit string.

    EDGAR APIs want a 10-digit zero-padded CIK. CM stores it variously
    (with/without leading zeros, sometimes a 'CIK' prefix). Empty -> ''.
    """
    s = (raw or "").strip()
    if not s:
        return ""
    s = s.upper().removeprefix("CIK").lstrip("0") or "0"
    if not s.isdigit():
        return ""
    return s.zfill(10)


def derive_filer_type(row: dict) -> str:
    """domestic (10-K filer) vs foreign (20-F/IFRS filer).

    The forensic rubric is built on 10-K disclosures (financial-statement notes,
    MD&A). ADR / cross-listed foreign private issuers file a 20-F with different
    structure and often IFRS, which the 10-K-based rubric cannot evaluate
    cleanly — so they must be routed to manual review (Data Gap tier), NOT
    silently screened as Green. We TAG rather than drop so coverage stays visible.
    """
    iso = (row.get("Country (ISO)", "") or "").strip().upper()
    listing = (row.get("Listing Type", "") or "").strip().lower()
    if (iso and iso != "USA") or "adr" in listing or "cross-listed" in listing:
        return "foreign"
    return "domestic"


def load_existing_watchlist() -> dict[str, dict]:
    if not WATCHLIST_CSV.exists():
        return {}
    with WATCHLIST_CSV.open(encoding="utf-8", newline="") as f:
        reader = csv.DictReader(f)
        return {row["ticker"]: row for row in reader if row.get("ticker")}


def load_coverage(coverage_csv: Path, large_cap: set[str] | None = None) -> list[dict]:
    large_cap = large_cap or set()
    rows = []
    with coverage_csv.open(encoding="utf-8", newline="") as f:
        reader = csv.DictReader(f)
        for row in reader:
            if not row.get("CIK"):
                continue
            sector = row.get("Sector (JP)", "").strip()
            if sector in EXCLUDED_SECTORS:
                continue
            if sector not in SUBGROUP_MAP:
                continue
            # Biopharma only if large-cap (S&P 500) — see LARGE_CAP_ONLY_SECTORS.
            if sector in LARGE_CAP_ONLY_SECTORS:
                ticker = (row.get("Ticker", "") or "").strip().upper()
                if ticker not in large_cap:
                    continue
            rows.append(row)
    return rows


def build_new_watchlist(
    coverage_rows: list[dict],
    existing: dict[str, dict],
    today: str,
) -> tuple[list[dict], list[str], list[str], list[tuple[str, str, str]]]:
    """Returns (new_rows, added_tickers, removed_tickers, subgroup_changes)."""
    new_rows: list[dict] = []
    seen: set[str] = set()
    added: list[str] = []
    subgroup_changes: list[tuple[str, str, str]] = []

    for row in coverage_rows:
        ticker = row["Ticker"].strip()
        if not ticker or ticker in seen:
            continue
        seen.add(ticker)

        sector = row["Sector (JP)"].strip()
        subgroup = SUBGROUP_MAP[sector]
        prev = existing.get(ticker)

        new_row = {
            "ticker": ticker,
            "company_name": row.get("Company Name", "").strip(),
            "cik": normalize_cik(row.get("CIK", "")),
            "sector_subgroup": subgroup,
            "subsector": row.get("Subsector (JP)", "").strip(),
            "core": row.get("Core", "").strip(),
            "filer_type": derive_filer_type(row),
            "added_date": prev["added_date"] if prev and prev.get("added_date") else today,
            "notes": prev.get("notes", "") if prev else "",
            "source": "cm",
        }
        new_rows.append(new_row)

        if not prev:
            added.append(ticker)
        elif prev.get("sector_subgroup") and prev["sector_subgroup"] != subgroup:
            subgroup_changes.append((ticker, prev["sector_subgroup"], subgroup))

    removed = sorted(set(existing) - seen)
    new_rows.sort(key=lambda r: (r["sector_subgroup"], r["ticker"]))
    return new_rows, sorted(added), removed, subgroup_changes


def load_sec_ticker_ciks() -> dict[str, str]:
    """Return {TICKER: zero-padded-10-digit CIK} from SEC's ticker map.

    Cached at SEC_TICKERS_CACHE; fetched once (free, no key — just a descriptive
    User-Agent per SEC policy). On any fetch failure with no cache, returns {} and
    the extras step degrades loudly (names without a CIK can't be EDGAR-screened).
    """
    data = None
    if SEC_TICKERS_CACHE.exists():
        try:
            data = json.loads(SEC_TICKERS_CACHE.read_text(encoding="utf-8"))
        except Exception:
            data = None
    if data is None:
        ua = os.environ.get("EDGAR_IDENTITY") or "Jason Peterson jroypeterson@gmail.com"
        req = urllib.request.Request(SEC_TICKERS_URL, headers={"User-Agent": ua})
        try:
            with urllib.request.urlopen(req, timeout=30) as resp:
                data = json.loads(resp.read().decode("utf-8"))
            SEC_TICKERS_CACHE.write_text(json.dumps(data), encoding="utf-8")
        except Exception as e:
            print(f"WARNING: could not fetch SEC ticker map ({e}); "
                  f"S&P 500 extras without a cached CIK will be skipped.", file=sys.stderr)
            return {}
    out: dict[str, str] = {}
    for rec in data.values():
        t = str(rec.get("ticker", "")).strip().upper()
        cik = rec.get("cik_str")
        if t and cik is not None:
            out[t] = str(cik).zfill(10)
    return out


def load_cm_master_tickers(coverage_csv: Path) -> set[str]:
    """Every ticker in the CM master (pre-filter), so the S&P 500 expansion only
    ADDS names CM doesn't already carry — it never re-introduces a name CM
    deliberately filtered out (e.g. a Biopharma name)."""
    out: set[str] = set()
    with coverage_csv.open(encoding="utf-8", newline="") as f:
        for row in csv.DictReader(f):
            t = (row.get("Ticker", "") or "").strip().upper()
            if t:
                out.add(t)
    return out


def build_sp500_extras(
    existing: dict[str, dict],
    cm_master: set[str],
    already_added: set[str],
    today: str,
) -> tuple[list[dict], list[str], list[str]]:
    """Rows for the S&P 500 expansion ring: roster-priority names NOT in the CM
    master. Returns (rows, added_tickers, unresolved_no_cik)."""
    try:
        import coverage_cohorts as cc
    except Exception as e:
        print(f"WARNING: coverage_cohorts unavailable ({e}); skipping S&P 500 expansion.",
              file=sys.stderr)
        return [], [], []

    rosters = cc.load_rosters()
    # The expansion ring is the S&P 500 specifically. Portfolio/Researching/Core are
    # already CM data (in cm_master); only the S&P 500 adds names CM doesn't carry.
    # (Using just the sp500 roster avoids pulling in foreign CM-position tickers whose
    # export format differs from the CM master, which have no SEC CIK anyway.)
    core_set = rosters["core"]
    # Compare on the canonical key so a class-ticker CM already carries (BRK-B) isn't
    # re-added under sigma's spelling (BRK.B).
    cm_canon = {ckey(t) for t in cm_master}
    added_canon = {ckey(t) for t in already_added}
    extras = sorted(t for t in rosters["sp500"]
                    if ckey(t) not in cm_canon and ckey(t) not in added_canon)

    sec_ciks = load_sec_ticker_ciks() if extras else {}
    rows: list[dict] = []
    added: list[str] = []
    unresolved: list[str] = []
    for ticker in extras:
        prev = existing.get(ticker)
        # Prefer a fresh SEC resolution, but fall back to the already-known CIK on an
        # existing row so a transient SEC-map outage can't DROP established S&P rows
        # (which would then be reported "removed" and written out of the watchlist).
        cik = (sec_ciks.get(ticker) or sec_ciks.get(ticker.replace(".", "-"))
               or (prev.get("cik") if prev and prev.get("cik") else ""))
        if not cik:
            unresolved.append(ticker)
            continue  # no CIK -> can't EDGAR-screen; skip loudly (reported below)
        rows.append({
            "ticker": ticker,
            "company_name": prev.get("company_name", "") if prev else "",
            "cik": cik,
            "sector_subgroup": "general",
            "subsector": "",
            "core": "Y" if ticker in core_set else "",
            "filer_type": "domestic",  # S&P 500 constituents are US 10-K filers
            "added_date": prev["added_date"] if prev and prev.get("added_date") else today,
            "notes": prev.get("notes", "") if prev else "",
            "source": "sp500",
        })
        if not prev:
            added.append(ticker)
    return rows, sorted(added), sorted(unresolved)


def write_watchlist(rows: list[dict]) -> None:
    WATCHLIST_CSV.parent.mkdir(parents=True, exist_ok=True)
    with WATCHLIST_CSV.open("w", encoding="utf-8", newline="") as f:
        writer = csv.DictWriter(f, fieldnames=WATCHLIST_FIELDS)
        writer.writeheader()
        writer.writerows(rows)


def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--dry-run", action="store_true", help="Report changes without writing")
    parser.add_argument("--no-sp500", action="store_true",
                        help="Skip the S&P 500 expansion ring (CM universe only)")
    args = parser.parse_args()

    coverage_csv = resolve_coverage_csv()
    if not coverage_csv.exists():
        print(f"ERROR: coverage CSV not found at {coverage_csv}", file=sys.stderr)
        print("Set FORENSIC_COVERAGE_CSV env var or check sibling-folder layout.", file=sys.stderr)
        return 1

    existing = load_existing_watchlist()
    try:
        import coverage_cohorts as cc
        large_cap = cc.load_sp500()  # S&P 500 = large-cap proxy for biopharma inclusion
    except Exception:
        large_cap = set()
    coverage_rows = load_coverage(coverage_csv, large_cap)
    today = date.today().isoformat()
    new_rows, added, removed, subgroup_changes = build_new_watchlist(
        coverage_rows, existing, today
    )

    # S&P 500 expansion ring: add roster names CM doesn't already carry.
    sp_added: list[str] = []
    sp_unresolved: list[str] = []
    if not args.no_sp500:
        cm_master = load_cm_master_tickers(coverage_csv)
        already = {r["ticker"] for r in new_rows}
        sp_rows, sp_added, sp_unresolved = build_sp500_extras(
            existing, cm_master, already, today
        )
        new_rows.extend(sp_rows)
        new_rows.sort(key=lambda r: (r["sector_subgroup"], r["ticker"]))

    # Recompute removals against the FINAL set (build_new_watchlist only saw the CM
    # pass, so without this the S&P 500 rows the extras step re-adds show as removed).
    final_tickers = {r["ticker"] for r in new_rows}
    removed = sorted(set(existing) - final_tickers)

    # Bake the cohort onto every row (+ disjoint roster totals) so CI — which has no
    # CM/sigma sibling repos — reads cohorts from the committed watchlist, not live.
    # The rosters are REQUIRED for a correct local sync: if the sibling exports are
    # missing/empty, cohort_for would stamp everything "other"/"sp500" and bake zeros
    # into cohort_totals.json — poisoned columns CI then trusts. So abort the write
    # rather than persist a mis-partitioned universe.
    cohort_totals: dict[str, int] = {}
    try:
        import coverage_cohorts as cc
        rosters = cc.load_rosters()
    except Exception as e:
        print(f"ERROR: coverage_cohorts unavailable ({e}); cannot bake cohorts — refusing "
              f"to write a watchlist without them.", file=sys.stderr)
        return 1
    missing = [c for c in ("portfolio", "core", "sp500") if not rosters.get(c)]
    if missing:
        print(f"ERROR: coverage rosters empty/missing: {', '.join(missing)}. The CM exports "
              f"(portfolio.json/universe_metadata.json) + sigma-alert/sources/sp500.txt are "
              f"REQUIRED for a correct local sync. Refusing to bake poisoned cohorts into "
              f"watchlist.csv — check the sibling repos.", file=sys.stderr)
        return 1
    for r in new_rows:
        r["cohort"] = cc.cohort_for(r["ticker"], rosters)
    p, rr, co, sp = (rosters["portfolio"], rosters["researching"],
                     rosters["core"], rosters["sp500"])
    cohort_totals = {
        "portfolio": len(p), "researching": len(rr - p),
        "core": len(co - rr - p), "sp500": len(sp - co - rr - p),
    }

    by_subgroup: dict[str, int] = {}
    for r in new_rows:
        by_subgroup[r["sector_subgroup"]] = by_subgroup.get(r["sector_subgroup"], 0) + 1

    print(f"Coverage source: {coverage_csv}")
    print(f"Watchlist target: {WATCHLIST_CSV}")
    print(f"Total in new watchlist: {len(new_rows)}")
    for sg in sorted(by_subgroup):
        print(f"  {sg}: {by_subgroup[sg]}")
    foreign = sum(1 for r in new_rows if r["filer_type"] == "foreign")
    print(f"Foreign (20-F/ADR -> Data Gap manual review): {foreign} of {len(new_rows)}")
    n_cm = sum(1 for r in new_rows if r.get("source") == "cm")
    n_sp = sum(1 for r in new_rows if r.get("source") == "sp500")
    print(f"By source: cm={n_cm}  sp500={n_sp}")
    print(f"Added (CM): {len(added)}")
    print(f"Added (S&P 500 ring): {len(sp_added)}")
    if sp_unresolved:
        print(f"S&P 500 names skipped (no CIK in SEC map): {len(sp_unresolved)} "
              f"-> {', '.join(sp_unresolved[:15])}{' ...' if len(sp_unresolved) > 15 else ''}")
    print(f"Removed: {len(removed)}")
    print(f"Subgroup changes: {len(subgroup_changes)}")

    if added:
        print("\n+ Added:")
        for t in added[:50]:
            print(f"  + {t}")
        if len(added) > 50:
            print(f"  ... and {len(added) - 50} more")
    if removed:
        print("\n- Removed:")
        for t in removed[:50]:
            print(f"  - {t}")
        if len(removed) > 50:
            print(f"  ... and {len(removed) - 50} more")
    if subgroup_changes:
        print("\n~ Subgroup changes:")
        for t, old, new in subgroup_changes[:50]:
            print(f"  ~ {t}: {old} -> {new}")

    if args.dry_run:
        print("\n[dry-run] no file written")
        return 0

    write_watchlist(new_rows)

exec
/bin/bash -lc "sed -n '1,180p' tests/test_sync_cohorts.py" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
"""Regression tests for the S&P 500 sync + cohort wiring (Codex round-1 findings)."""
from __future__ import annotations

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

import coverage_cohorts as cc  # noqa: E402
import next_batch as nb  # noqa: E402
import sync_watchlist as sw  # noqa: E402


def test_ckey_folds_class_punctuation():
    # BRK.B / BRK-B are the same security across sources; GOOG vs GOOGL are not.
    assert sw.ckey("BRK.B") == "BRK-B"
    assert sw.ckey("brk.b") == "BRK-B"
    assert sw.ckey("GOOGL") == "GOOGL"
    assert sw.ckey("GOOG") != sw.ckey("GOOGL")


def test_sort_key_defensive_without_rosters():
    # Codex Critical: run_unattended called sort_key() with no rosters -> TypeError.
    # sort_key must be callable without rosters (uses the baked cohort column).
    k = nb.sort_key({"ticker": "AAA", "cohort": "portfolio"})
    assert k[0] == cc.COHORT_RANK["portfolio"]
    k2 = nb.sort_key({"ticker": "ZZZ", "cohort": "sp500"})
    assert k2[0] == cc.COHORT_RANK["sp500"]
    assert k < k2  # portfolio sorts before sp500


def test_sp500_extras_preserves_cik_on_sec_failure(monkeypatch):
    # Codex High: a transient SEC-map outage must NOT drop an existing S&P row
    # (which would then be reported "removed" and written out of the watchlist).
    monkeypatch.setattr(cc, "load_rosters", lambda: {
        "portfolio": set(), "researching": set(), "core": set(), "sp500": {"ABNB"}})
    monkeypatch.setattr(sw, "load_sec_ticker_ciks", lambda: {})  # SEC map unavailable
    existing = {"ABNB": {"cik": "0001559720", "added_date": "2026-01-01",
                         "company_name": "Airbnb"}}
    rows, added, unresolved = sw.build_sp500_extras(
        existing, cm_master=set(), already_added=set(), today="2026-07-13")
    assert unresolved == []                       # NOT dropped
    assert len(rows) == 1 and rows[0]["cik"] == "0001559720"


def test_sp500_extras_dedup_class_ticker(monkeypatch):
    # Codex High: CM carrying BRK-B must suppress re-adding sigma's BRK.B spelling.
    monkeypatch.setattr(cc, "load_rosters", lambda: {
        "portfolio": set(), "researching": set(), "core": set(), "sp500": {"BRK.B"}})
    monkeypatch.setattr(sw, "load_sec_ticker_ciks", lambda: {"BRK-B": "0001067983"})
    rows, added, unresolved = sw.build_sp500_extras(
        {}, cm_master={"BRK-B"}, already_added=set(), today="2026-07-13")
    assert rows == [] and added == []             # no duplicate row


def test_sp500_extras_new_name_without_cik_is_skipped(monkeypatch):
    # A brand-new S&P name with no prev row and no SEC CIK is skipped loudly (can't screen).
    monkeypatch.setattr(cc, "load_rosters", lambda: {
        "portfolio": set(), "researching": set(), "core": set(), "sp500": {"NEWCO"}})
    monkeypatch.setattr(sw, "load_sec_ticker_ciks", lambda: {})
    rows, added, unresolved = sw.build_sp500_extras(
        {}, cm_master=set(), already_added=set(), today="2026-07-13")
    assert rows == [] and unresolved == ["NEWCO"]

exec
/bin/bash -lc 'git status --short' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 348ms:
 M .github/workflows/forensic_triage.yml
 M .gitignore
 M CLAUDE.md
 M PROJECT_BRIEF.md
 M codex_feedback/codex_feedback_2026-07-08_165729.md
 M codex_feedback/codex_feedback_2026-07-08_170704.md
 M codex_feedback/codex_feedback_2026-07-13_210219.md
 M coverage_cohorts.py
 M dashboard.py
 M data/cohort_totals.json
 M data/watchlist.csv
 M edgar_fetch.py
 M next_batch.py
 M reports/coverage_dashboard.html
 M reports/forensic_2026-04-17_smoketest.md
 M reports/forensic_2026-06-25.md
 M reports/forensic_2026-06-29.md
 M reports/forensic_2026-06-30.md
 M reports/forensic_2026-07-03.md
 M reports/forensic_2026-07-08.md
 M reports/forensic_2026-07-09.md
 M reports/forensic_2026-07-10.md
 M reports/forensic_2026-07-13.md
 M sync_watchlist.py
 M tests/test_dashboard.py
 M tests/test_edgar_fetch.py
?? codex_feedback/codex_feedback_2026-07-13_211218.md

codex
I’ve confirmed the patched call paths are present. I’m checking the rest of the sync write path now, because the key risk is whether the new guards run before any persistent output and whether S&P rows are compared under the same ticker spelling everywhere.
exec
/bin/bash -lc "sed -n '430,520p' sync_watchlist.py" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
    write_watchlist(new_rows)
    print(f"\nWrote {len(new_rows)} rows to {WATCHLIST_CSV}")
    if cohort_totals:
        COHORT_TOTALS_JSON.write_text(json.dumps(cohort_totals, indent=2), encoding="utf-8")
        print(f"Wrote cohort totals to {COHORT_TOTALS_JSON}: {cohort_totals}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())

exec
/bin/bash -lc 'pytest -q tests/test_sync_cohorts.py -p no:cacheprovider' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
exec
/bin/bash -lc "rg -n \"sort_key\\(|row_cohort\\(|build_sp500_extras\\(|load_rosters\\(|cohort_for\\(|ckey\\(|existing\\.get\\(|removed =|write_watchlist|COHORT_TOTALS_JSON\" ." in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 110ms:
./codex_feedback/codex_feedback_2026-07-13_211218.md:31:1. CRITICAL — `run_unattended._pick_batch` called `next_batch.sort_key()` without the required `rosters` arg → TypeError. Fix: pass `rosters = nb.cc.load_rosters()` into a `lambda r: nb.sort_key(r, rosters)`, and default `sort_key(row, rosters=None)` so it can't crash again (relies on the baked `cohort` column / `cohort_for` live-load when rosters is None/empty).
./codex_feedback/codex_feedback_2026-07-13_211218.md:33:3. HIGH — class-ticker punctuation (BRK.B vs BRK-B) could double-add a name. Fix: new `ckey()` folds `.`→`-`; `cm_master`/`already_added` compared on `ckey`.
./codex_feedback/codex_feedback_2026-07-13_211218.md:37:- Does the `run_unattended` fix actually order correctly in CI where `load_rosters()` returns empty sets? (The baked `cohort` column should carry it — confirm `sort_key`→`row_cohort` reads the baked column before falling back.)
./codex_feedback/codex_feedback_2026-07-13_211218.md:54:-def row_cohort(row: dict, rosters: dict[str, set[str]]) -> str:
./codex_feedback/codex_feedback_2026-07-13_211218.md:56:+def row_cohort(row: dict, rosters: dict[str, set[str]] | None = None) -> str:
./codex_feedback/codex_feedback_2026-07-13_211218.md:61:     return c if c else cc.cohort_for(row.get("ticker", ""), rosters)
./codex_feedback/codex_feedback_2026-07-13_211218.md:64:-def sort_key(row: dict, rosters: dict[str, set[str]]):
./codex_feedback/codex_feedback_2026-07-13_211218.md:65:+def sort_key(row: dict, rosters: dict[str, set[str]] | None = None):
./codex_feedback/codex_feedback_2026-07-13_211218.md:66:     cohort_rank = cc.COHORT_RANK.get(row_cohort(row, rosters), 99)
./codex_feedback/codex_feedback_2026-07-13_211218.md:77:+    rosters = nb.cc.load_rosters()  # empty in CI; sort_key falls back to the baked `cohort` column
./codex_feedback/codex_feedback_2026-07-13_211218.md:82:+    pending.sort(key=lambda r: nb.sort_key(r, rosters))
./codex_feedback/codex_feedback_2026-07-13_211218.md:91: COHORT_TOTALS_JSON = ROOT / "data" / "cohort_totals.json"
./codex_feedback/codex_feedback_2026-07-13_211218.md:94:+def ckey(t: str) -> str:
./codex_feedback/codex_feedback_2026-07-13_211218.md:105:@@ -258,19 +266,27 @@ def build_sp500_extras(
./codex_feedback/codex_feedback_2026-07-13_211218.md:111:+    cm_canon = {ckey(t) for t in cm_master}
./codex_feedback/codex_feedback_2026-07-13_211218.md:112:+    added_canon = {ckey(t) for t in already_added}
./codex_feedback/codex_feedback_2026-07-13_211218.md:115:+                    if ckey(t) not in cm_canon and ckey(t) not in added_canon)
./codex_feedback/codex_feedback_2026-07-13_211218.md:123:+        prev = existing.get(ticker)
./codex_feedback/codex_feedback_2026-07-13_211218.md:132:-        prev = existing.get(ticker)
./codex_feedback/codex_feedback_2026-07-13_211218.md:147:         rosters = cc.load_rosters()
./codex_feedback/codex_feedback_2026-07-13_211218.md:149:-            r["cohort"] = cc.cohort_for(r["ticker"], rosters)
./codex_feedback/codex_feedback_2026-07-13_211218.md:172:+        r["cohort"] = cc.cohort_for(r["ticker"], rosters)
./codex_feedback/codex_feedback_2026-07-13_211218.md:203:+    assert sw.ckey("BRK.B") == "BRK-B"
./codex_feedback/codex_feedback_2026-07-13_211218.md:204:+    assert sw.ckey("brk.b") == "BRK-B"
./codex_feedback/codex_feedback_2026-07-13_211218.md:205:+    assert sw.ckey("GOOGL") == "GOOGL"
./codex_feedback/codex_feedback_2026-07-13_211218.md:206:+    assert sw.ckey("GOOG") != sw.ckey("GOOGL")
./codex_feedback/codex_feedback_2026-07-13_211218.md:210:+    # Codex Critical: run_unattended called sort_key() with no rosters -> TypeError.
./codex_feedback/codex_feedback_2026-07-13_211218.md:212:+    k = nb.sort_key({"ticker": "AAA", "cohort": "portfolio"})
./codex_feedback/codex_feedback_2026-07-13_211218.md:214:+    k2 = nb.sort_key({"ticker": "ZZZ", "cohort": "sp500"})
./codex_feedback/codex_feedback_2026-07-13_211218.md:227:+    rows, added, unresolved = sw.build_sp500_extras(
./codex_feedback/codex_feedback_2026-07-13_211218.md:238:+    rows, added, unresolved = sw.build_sp500_extras(
./codex_feedback/codex_feedback_2026-07-13_211218.md:248:+    rows, added, unresolved = sw.build_sp500_extras(
./codex_feedback/codex_feedback_2026-07-13_211218.md:314:    rosters = nb.cc.load_rosters()  # empty in CI; sort_key falls back to the baked `cohort` column
./codex_feedback/codex_feedback_2026-07-13_211218.md:318:    pending.sort(key=lambda r: nb.sort_key(r, rosters))
./codex_feedback/codex_feedback_2026-07-13_211218.md:481:def load_rosters() -> dict[str, set[str]]:
./codex_feedback/codex_feedback_2026-07-13_211218.md:491:def cohort_for(ticker: str, rosters: dict[str, set[str]] | None = None) -> str:
./codex_feedback/codex_feedback_2026-07-13_211218.md:494:        rosters = load_rosters()
./codex_feedback/codex_feedback_2026-07-13_211218.md:506:        rosters = load_rosters()
./codex_feedback/codex_feedback_2026-07-13_211218.md:507:    return {_norm(t): cohort_for(t, rosters) for t in tickers}
./codex_feedback/codex_feedback_2026-07-13_211218.md:510:def sort_key(cohort: str, ticker: str) -> tuple:
./codex_feedback/codex_feedback_2026-07-13_211218.md:584:def row_cohort(row: dict, rosters: dict[str, set[str]] | None = None) -> str:
./codex_feedback/codex_feedback_2026-07-13_211218.md:589:    return c if c else cc.cohort_for(row.get("ticker", ""), rosters)
./codex_feedback/codex_feedback_2026-07-13_211218.md:592:def sort_key(row: dict, rosters: dict[str, set[str]] | None = None):
./codex_feedback/codex_feedback_2026-07-13_211218.md:593:    cohort_rank = cc.COHORT_RANK.get(row_cohort(row, rosters), 99)
./codex_feedback/codex_feedback_2026-07-13_211218.md:606:    rosters = cc.load_rosters()
./codex_feedback/codex_feedback_2026-07-13_211218.md:612:    pending.sort(key=lambda r: sort_key(r, rosters))
./codex_feedback/codex_feedback_2026-07-13_211218.md:627:        cohort = row_cohort(r, rosters)
./codex_feedback/codex_feedback_2026-07-13_211218.md:722:COHORT_TOTALS_JSON = ROOT / "data" / "cohort_totals.json"
./codex_feedback/codex_feedback_2026-07-13_211218.md:725:def ckey(t: str) -> str:
./codex_feedback/codex_feedback_2026-07-13_211218.md:813:        prev = existing.get(ticker)
./codex_feedback/codex_feedback_2026-07-13_211218.md:834:    removed = sorted(set(existing) - seen)
./codex_feedback/codex_feedback_2026-07-13_211218.md:885:def build_sp500_extras(
./codex_feedback/codex_feedback_2026-07-13_211218.md:900:    rosters = cc.load_rosters()
./codex_feedback/codex_feedback_2026-07-13_211218.md:908:    cm_canon = {ckey(t) for t in cm_master}
./codex_feedback/codex_feedback_2026-07-13_211218.md:909:    added_canon = {ckey(t) for t in already_added}
./codex_feedback/codex_feedback_2026-07-13_211218.md:911:                    if ckey(t) not in cm_canon and ckey(t) not in added_canon)
./codex_feedback/codex_feedback_2026-07-13_211218.md:918:        prev = existing.get(ticker)
./codex_feedback/codex_feedback_2026-07-13_211218.md:944:def write_watchlist(rows: list[dict]) -> None:
./codex_feedback/codex_feedback_2026-07-13_211218.md:983:        sp_rows, sp_added, sp_unresolved = build_sp500_extras(
./codex_feedback/codex_feedback_2026-07-13_211218.md:992:    removed = sorted(set(existing) - final_tickers)
./codex_feedback/codex_feedback_2026-07-13_211218.md:1003:        rosters = cc.load_rosters()
./codex_feedback/codex_feedback_2026-07-13_211218.md:1016:        r["cohort"] = cc.cohort_for(r["ticker"], rosters)
./codex_feedback/codex_feedback_2026-07-13_211218.md:1067:    write_watchlist(new_rows)
./codex_feedback/codex_feedback_2026-07-13_211218.md:1087:    assert sw.ckey("BRK.B") == "BRK-B"
./codex_feedback/codex_feedback_2026-07-13_211218.md:1088:    assert sw.ckey("brk.b") == "BRK-B"
./codex_feedback/codex_feedback_2026-07-13_211218.md:1089:    assert sw.ckey("GOOGL") == "GOOGL"
./codex_feedback/codex_feedback_2026-07-13_211218.md:1090:    assert sw.ckey("GOOG") != sw.ckey("GOOGL")
./codex_feedback/codex_feedback_2026-07-13_211218.md:1094:    # Codex Critical: run_unattended called sort_key() with no rosters -> TypeError.
./codex_feedback/codex_feedback_2026-07-13_211218.md:1096:    k = nb.sort_key({"ticker": "AAA", "cohort": "portfolio"})
./codex_feedback/codex_feedback_2026-07-13_211218.md:1098:    k2 = nb.sort_key({"ticker": "ZZZ", "cohort": "sp500"})
./codex_feedback/codex_feedback_2026-07-13_211218.md:1111:    rows, added, unresolved = sw.build_sp500_extras(
./codex_feedback/codex_feedback_2026-07-13_211218.md:1122:    rows, added, unresolved = sw.build_sp500_extras(
./tests/test_dashboard.py:19:    assert cc.cohort_for("AAA", rosters) == "portfolio"
./tests/test_dashboard.py:24:    assert cc.cohort_for("R", rosters) == "researching"
./tests/test_dashboard.py:25:    assert cc.cohort_for("C", rosters) == "core"
./tests/test_dashboard.py:26:    assert cc.cohort_for("S", rosters) == "sp500"
./tests/test_dashboard.py:27:    assert cc.cohort_for("Z", rosters) == "other"  # in no ring
./tests/test_dashboard.py:32:    assert cc.cohort_for("aaa", rosters) == "portfolio"

 exited 1 in 336ms:
Traceback (most recent call last):
  File "/home/jroyp/.local/bin/pytest", line 6, in <module>
    sys.exit(console_main())
             ^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/config/__init__.py", line 223, in console_main
    code = main()
           ^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/config/__init__.py", line 193, in main
    config = _prepareconfig(new_args, plugins)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/config/__init__.py", line 361, in _prepareconfig
    config: Config = pluginmanager.hook.pytest_cmdline_parse(
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/helpconfig.py", line 124, in pytest_cmdline_parse
    config = yield
             ^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_callers.py", line 121, in _multicall
    res = hook_impl.function(*args)
          ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/config/__init__.py", line 1192, in pytest_cmdline_parse
    self.parse(args)
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/config/__init__.py", line 1562, in parse
    self.hook.pytest_load_initial_conftests(
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/warnings.py", line 128, in pytest_load_initial_conftests
    return (yield)
            ^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/pluggy/_callers.py", line 116, in _multicall
    next(function_gen)  # first yield
    ^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/capture.py", line 170, in pytest_load_initial_conftests
    capman.start_global_capturing()
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/capture.py", line 773, in start_global_capturing
    self._global_capturing = _get_multicapture(self._method)
                             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/capture.py", line 714, in _get_multicapture
    return MultiCapture(in_=FDCapture(0), out=FDCapture(1), err=FDCapture(2))
                                              ^^^^^^^^^^^^
  File "/home/jroyp/.local/lib/python3.12/site-packages/_pytest/capture.py", line 493, in __init__
    TemporaryFile(buffering=0),
    ^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 770, in TemporaryFile
    prefix, suffix, dir, output_type = _sanitize_params(prefix, suffix, dir)
                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 265, in _sanitize_params
    dir = gettempdir()
          ^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 454, in gettempdir
    return _os.fsdecode(_gettempdir())
                        ^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 447, in _gettempdir
    tempdir = _get_default_tempdir()
              ^^^^^^^^^^^^^^^^^^^^^^
  File "/usr/lib/python3.12/tempfile.py", line 362, in _get_default_tempdir
    raise FileNotFoundError(_errno.ENOENT,
FileNotFoundError: [Errno 2] No usable temporary directory found in ['/tmp', '/var/tmp', '/usr/tmp', '/mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage']

codex
The regression test file is structurally targeted, but `pytest` cannot start in this read-only environment because Python cannot create any temp file for capture. I’ll still validate the behaviors statically and with direct import-level checks where possible without writing.
exec
/bin/bash -lc 'head -n 3 data/watchlist.csv' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
ticker,company_name,cik,sector_subgroup,subsector,core,filer_type,added_date,notes,source,cohort
AAPL,Apple Inc.,0000320193,general,,Y,domestic,2026-04-07,,cm,portfolio
ABBV,AbbVie Common Stock,0001551152,general,Large Pharma,,domestic,2026-07-13,,cm,sp500

exec
/bin/bash -lc 'rg -n "''^(BRK|BF)[.-]|,BRK[.-]|,BF[.-]|cohort" data/watchlist.csv' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
1:ticker,company_name,cik,sector_subgroup,subsector,core,filer_type,added_date,notes,source,cohort
59:BF-B,,0000014693,general,,,domestic,2026-07-13,,sp500,sp500
73:BRK-B,,0001067983,general,,,domestic,2026-07-13,,sp500,sp500

exec
/bin/bash -lc 'rg -n "sp500|coverage_cohorts|sync_watchlist|run_unattended|next_batch" .github/workflows/forensic_triage.yml' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
51:        run: python run_unattended.py --batch-size "$BATCH_SIZE" --run-id "${{ github.run_id }}"
89:        run: python run_unattended.py --notify-only --run-id "${{ github.run_id }}"
93:        run: python run_unattended.py --failure-alarm --run-id "${{ github.run_id }}" --error "workflow step failed (see Actions log)"

exec
/bin/bash -lc "PYTHONDONTWRITEBYTECODE=1 python - <<'PY'
import coverage_cohorts as cc
import next_batch as nb
import sync_watchlist as sw

print('sort_empty_rosters', nb.sort_key({'ticker':'ZZZ','cohort':'sp500'}, {'portfolio':set(),'researching':set(),'core':set(),'sp500':set()}))
print('sort_none_rosters', nb.sort_key({'ticker':'AAA','cohort':'portfolio'}))
print('row_no_cohort_empty', nb.row_cohort({'ticker':'AAA'}, {'portfolio':set(),'researching':set(),'core':set(),'sp500':set()}))
print('ckey', sw.ckey('BRK.B'), sw.ckey('BRK-B'))
PY" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 exited 127 in 118ms:
/bin/bash: line 1: python: command not found

codex
The committed watchlist does have the baked `cohort` column, and the class-share rows currently use dash spelling. I hit `python` not being on PATH, so I’m rerunning the direct import checks with `python3`.
exec
/bin/bash -lc "PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'
import coverage_cohorts as cc
import sync_watchlist as sw

sw.load_sec_ticker_ciks = lambda: {}
cc.load_rosters = lambda: {'portfolio': set(), 'researching': set(), 'core': set(), 'sp500': {'ABNB'}}
rows, added, unresolved = sw.build_sp500_extras({'ABNB': {'cik':'0001559720','added_date':'2026-01-01','company_name':'Airbnb'}}, set(), set(), '2026-07-13')
print(rows)
print(added, unresolved)
PY" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
[{'ticker': 'ABNB', 'company_name': 'Airbnb', 'cik': '0001559720', 'sector_subgroup': 'general', 'subsector': '', 'core': '', 'filer_type': 'domestic', 'added_date': '2026-01-01', 'notes': '', 'source': 'sp500'}]
[] []

exec
/bin/bash -lc "PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'
import coverage_cohorts as cc
import next_batch as nb
import sync_watchlist as sw

empty = {'portfolio':set(),'researching':set(),'core':set(),'sp500':set()}
print('sort_empty_rosters_sp500', nb.sort_key({'ticker':'ZZZ','cohort':'sp500'}, empty))
print('sort_none_rosters_portfolio', nb.sort_key({'ticker':'AAA','cohort':'portfolio'}))
print('row_no_cohort_empty', nb.row_cohort({'ticker':'AAA'}, empty))
print('ckey_equal', sw.ckey('BRK.B') == sw.ckey('BRK-B'))
PY" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
sort_empty_rosters_sp500 (3, 9, 'ZZZ')
sort_none_rosters_portfolio (0, 9, 'AAA')
row_no_cohort_empty other
ckey_equal True

exec
/bin/bash -lc "PYTHONDONTWRITEBYTECODE=1 python3 - <<'PY'
import coverage_cohorts as cc
import sync_watchlist as sw

sw.load_sec_ticker_ciks = lambda: {'BRK-B':'0001067983'}
cc.load_rosters = lambda: {'portfolio': set(), 'researching': set(), 'core': set(), 'sp500': {'BRK.B'}}
rows, added, unresolved = sw.build_sp500_extras({}, {'BRK-B'}, set(), '2026-07-13')
print(rows, added, unresolved)
PY" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
[] [] []

codex
The direct checks confirm the important behaviors without needing pytest: baked `cohort` wins even when the passed roster sets are empty, existing ABNB keeps its previous CIK on an empty SEC map, and BRK dot/dash canonicalization suppresses the duplicate case covered by the new test.
exec
/bin/bash -lc "sed -n '1,280p' dashboard.py" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
"""forensic_triage — coverage dashboard.

An ongoing "how far through the universe am I" view, modeled on the transcripts
daily digest. Forensic triage is a *standing process*, not a one-shot task: names
are screened a few per day, and this dashboard shows coverage + freshness across
JP's priority rings so progress (and gaps) are always visible.

Rings (disjoint, priority order — see coverage_cohorts.py):
    Portfolio -> Researching -> Core coverage -> S&P 500  (+ Other residual)

"Screened" = the name has a `flags_history.csv` row dated >= the cycle start with
status=complete (the exact done-definition next_batch.py uses). Per ring we show
screened/total, 🔴/🟡 flag tallies, pending, and (foreign filers) the Data-Gap count
that can't be EDGAR-screened. A day-over-day delta is persisted so the digest shows
motion, and an ETA projects the full-sweep finish at the batch rate.

Surfaces:
  python dashboard.py                 # plaintext to stdout (dry-run; no post, no snapshot)
  python dashboard.py --html          # also write reports/coverage_dashboard.html
  python dashboard.py --post          # post Slack digest (#forensic-flags) + save snapshot
  python dashboard.py --post --html   # both
  python dashboard.py --per-day 6 --cycle-start 2026-06-20
"""
from __future__ import annotations

import argparse
import csv
import html as _html
import json
import os
import urllib.request
from datetime import date, datetime, timedelta, timezone
from pathlib import Path

import coverage_cohorts as cc

ROOT = Path(__file__).parent
WATCHLIST_CSV = ROOT / "data" / "watchlist.csv"
FLAGS_HISTORY_CSV = ROOT / "data" / "flags_history.csv"
HEALTH_DIR = ROOT / ".health"
HISTORY_PATH = HEALTH_DIR / "dashboard_history.json"
HTML_OUT = ROOT / "reports" / "coverage_dashboard.html"
COHORT_TOTALS_JSON = ROOT / "data" / "cohort_totals.json"

DEFAULT_CYCLE_START = "2026-06-20"
DEFAULT_PER_DAY = 6

SLACK_WEBHOOK_ENV = "SLACK_WEBHOOK_FORENSIC"

# Cohorts shown in the dashboard, priority order.
COHORTS = ["portfolio", "researching", "core", "sp500", "other"]
ACTIONABLE = ("Red", "Yellow")  # the tiers worth surfacing by name


# ---------------------------------------------------------------- data reads

def load_watchlist() -> list[dict]:
    if not WATCHLIST_CSV.exists():
        return []
    with WATCHLIST_CSV.open(encoding="utf-8", newline="") as f:
        return list(csv.DictReader(f))


def latest_screens(cycle_start: str) -> dict[str, dict]:
    """{TICKER: {tier, flag_details, run_date}} for the LATEST complete screen
    since cycle_start. A status=fetch_failed row does not count (mirrors
    next_batch.screened_since); legacy rows with no status column are complete."""
    out: dict[str, dict] = {}
    if not FLAGS_HISTORY_CSV.exists():
        return out
    with FLAGS_HISTORY_CSV.open(encoding="utf-8", newline="") as f:
        reader = csv.DictReader(f)
        has_status = "status" in (reader.fieldnames or [])
        for row in reader:
            rd = (row.get("run_date") or "").strip()
            tk = (row.get("ticker") or "").strip().upper()
            if not tk or rd < cycle_start:
                continue
            status = (row.get("status") or "").strip() if has_status else "complete"
            if status not in ("", "complete"):
                continue
            prev = out.get(tk)
            if prev is None or rd >= prev["run_date"]:
                out[tk] = {
                    "tier": (row.get("tier") or "").strip(),
                    "flag_details": (row.get("flag_details") or "").strip(),
                    "run_date": rd,
                }
    return out


# ---------------------------------------------------------------- gather

def gather(cycle_start: str = DEFAULT_CYCLE_START,
           per_day: int = DEFAULT_PER_DAY,
           today: str | None = None) -> dict:
    today = today or date.today().isoformat()
    watchlist = load_watchlist()
    rosters = cc.load_rosters()
    screens = latest_screens(cycle_start)

    def _cohort(row):
        c = (row.get("cohort") or "").strip()  # baked at sync (CI-safe)
        return c if c else cc.cohort_for(row.get("ticker", ""), rosters)

    # Per-cohort accumulators.
    blank = lambda: {
        "total": 0, "domestic": 0, "foreign": 0,
        "screened": 0, "pending": 0,
        "tiers": {"Red": 0, "Yellow": 0, "Green": 0,
                  "DataGap": 0, "CorporateAction": 0},
        "flagged": [],  # [{ticker, tier, concern}] for Red/Yellow
    }
    coh = {c: blank() for c in COHORTS}

    for r in watchlist:
        tk = (r.get("ticker") or "").strip().upper()
        if not tk:
            continue
        c = _cohort(r)
        b = coh[c]
        b["total"] += 1
        is_foreign = r.get("filer_type") == "foreign"
        if is_foreign:
            b["foreign"] += 1
        else:
            b["domestic"] += 1
        sc = screens.get(tk)
        if sc:
            b["screened"] += 1
            tier = sc["tier"] if sc["tier"] in b["tiers"] else "Green"
            b["tiers"][tier] = b["tiers"].get(tier, 0) + 1
            if sc["tier"] in ACTIONABLE:
                b["flagged"].append({
                    "ticker": tk, "tier": sc["tier"],
                    "concern": _first_concern(sc["flag_details"]),
                })
        elif not is_foreign:
            b["pending"] += 1

    # Disjoint roster sizes = what SHOULD be in each ring, INCLUDING names forensic
    # can't screen (biopharma is excluded at sync; foreign is Data-Gap). Surfacing
    # `excluded` keeps the denominator honest — a hidden 10-name gap on Portfolio
    # reads as "covered" otherwise.
    roster_size = _load_cohort_totals()
    if roster_size is None:  # no baked totals -> compute live from rosters
        p, rr, co, sp = (rosters["portfolio"], rosters["researching"],
                         rosters["core"], rosters["sp500"])
        roster_size = {
            "portfolio": len(p),
            "researching": len(rr - p),
            "core": len(co - rr - p),
            "sp500": len(sp - co - rr - p),
        }
    roster_size = dict(roster_size)
    roster_size["other"] = coh["other"]["total"]
    for c in COHORTS:
        rsize = roster_size.get(c, coh[c]["total"])
        coh[c]["roster"] = rsize
        in_wl = coh[c]["domestic"] + coh[c]["foreign"]
        coh[c]["excluded"] = max(0, rsize - in_wl)

    # Per-ring ETA. Screening runs top-down by priority, so a ring is only complete
    # once everything ABOVE it plus its own pending is screened -> cumulative days.
    # A ring with 0 pending is already complete now (regardless of rings above it).
    cum_pending = 0
    for c in COHORTS:
        b = coh[c]
        cum_pending += b["pending"]
        if b["pending"] == 0:
            b["eta_days"] = 0
            b["eta_date"] = None  # already complete
        elif per_day > 0:
            days = -(-cum_pending // per_day)  # ceil of cumulative pending
            b["eta_days"] = days
            b["eta_date"] = (date.fromisoformat(today) + timedelta(days=days)).isoformat()
        else:
            b["eta_days"] = None
            b["eta_date"] = None

    # Overall roll-up (domestic denominators — foreign are Data-Gap, not screenable).
    dom_total = sum(b["domestic"] for b in coh.values())
    dom_screened = sum(b["screened"] for b in coh.values())
    dom_pending = sum(b["pending"] for b in coh.values())
    red = sum(b["tiers"]["Red"] for b in coh.values())
    yellow = sum(b["tiers"]["Yellow"] for b in coh.values())
    foreign_total = sum(b["foreign"] for b in coh.values())

    eta_date = None
    eta_days = None
    if dom_pending > 0 and per_day > 0:
        eta_days = -(-dom_pending // per_day)  # ceil
        eta_date = (date.fromisoformat(today) + timedelta(days=eta_days)).isoformat()

    # Flagged names across cohorts, priority order then Red-before-Yellow.
    flagged = []
    for c in COHORTS:
        reds = [f for f in coh[c]["flagged"] if f["tier"] == "Red"]
        yels = [f for f in coh[c]["flagged"] if f["tier"] == "Yellow"]
        for f in reds + yels:
            flagged.append({**f, "cohort": c})

    data = {
        "today": today,
        "cycle_start": cycle_start,
        "per_day": per_day,
        "cohorts": coh,
        "totals": {
            "domestic": dom_total, "screened": dom_screened,
            "pending": dom_pending, "foreign": foreign_total,
            "red": red, "yellow": yellow,
            "excluded": sum(b["excluded"] for b in coh.values()),
            "pct": (100.0 * dom_screened / dom_total) if dom_total else 0.0,
        },
        "eta_date": eta_date,
        "eta_days": eta_days,
        "flagged": flagged,
        "generated_at": datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ"),
    }
    data["diff"] = _diff_vs_prior(data)
    return data


def _load_cohort_totals() -> dict | None:
    """Baked disjoint roster sizes (CI-safe). None if absent -> compute live."""
    if not COHORT_TOTALS_JSON.exists():
        return None
    try:
        return json.loads(COHORT_TOTALS_JSON.read_text(encoding="utf-8"))
    except Exception:
        return None


def _first_concern(flag_details: str, limit: int = 80) -> str:
    if not flag_details:
        return ""
    s = flag_details.split(";")[0].split("|")[0].strip()
    return (s[: limit - 1] + "…") if len(s) > limit else s


# ---------------------------------------------------------------- day-over-day

def _snapshot_row(data: dict) -> dict:
    return {
        "date": data["today"],
        "screened": data["totals"]["screened"],
        "pending": data["totals"]["pending"],
        "red": data["totals"]["red"],
        "yellow": data["totals"]["yellow"],
        "by_cohort": {c: data["cohorts"][c]["screened"] for c in COHORTS},
    }


def _load_history() -> list[dict]:
    if not HISTORY_PATH.exists():
        return []
    try:
        return json.loads(HISTORY_PATH.read_text(encoding="utf-8"))
    except Exception:
        return []


def _diff_vs_prior(data: dict) -> dict | None:
    """Compare to the most recent snapshot from a DIFFERENT date."""
    hist = _load_history()
    prior = next((h for h in reversed(hist) if h.get("date") != data["today"]), None)
    if not prior:
        return None
    return {
        "date": prior["date"],
        "screened": data["totals"]["screened"] - prior.get("screened", 0),
        "red": data["totals"]["red"] - prior.get("red", 0),
        "yellow": data["totals"]["yellow"] - prior.get("yellow", 0),
    }


def save_snapshot(data: dict) -> None:
    HEALTH_DIR.mkdir(parents=True, exist_ok=True)
    hist = _load_history()
    hist = [h for h in hist if h.get("date") != data["today"]]  # replace today

exec
/bin/bash -lc "rg -n \"ckey|replace\\(\\\"\\.\\\", \\\"-\\\"\\)|replace\\('.', '-'\\)|BRK|BF-B|BF\\.B|company_tickers|sp500\" sync_watchlist.py coverage_cohorts.py data/watchlist.csv tests" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
sync_watchlist.py:33:SEC_TICKERS_URL = "https://www.sec.gov/files/company_tickers.json"
sync_watchlist.py:34:SEC_TICKERS_CACHE = ROOT / "data" / ".sec_company_tickers.json"
sync_watchlist.py:81:    "source",  # "cm" (Coverage Manager universe) or "sp500" (S&P 500 expansion ring)
sync_watchlist.py:82:    "cohort",  # portfolio|researching|core|sp500|other — baked so CI (no sibling repos) reads it
sync_watchlist.py:88:def ckey(t: str) -> str:
sync_watchlist.py:90:    on class-share punctuation (`BRK.B` vs `BRK-B`, `BF.B` vs `BF-B`); fold `.`→`-` so
sync_watchlist.py:93:    return (t or "").strip().upper().replace(".", "-")
sync_watchlist.py:248:def build_sp500_extras(
sync_watchlist.py:266:    # (Using just the sp500 roster avoids pulling in foreign CM-position tickers whose
sync_watchlist.py:269:    # Compare on the canonical key so a class-ticker CM already carries (BRK-B) isn't
sync_watchlist.py:270:    # re-added under sigma's spelling (BRK.B).
sync_watchlist.py:271:    cm_canon = {ckey(t) for t in cm_master}
sync_watchlist.py:272:    added_canon = {ckey(t) for t in already_added}
sync_watchlist.py:273:    extras = sorted(t for t in rosters["sp500"]
sync_watchlist.py:274:                    if ckey(t) not in cm_canon and ckey(t) not in added_canon)
sync_watchlist.py:285:        cik = (sec_ciks.get(ticker) or sec_ciks.get(ticker.replace(".", "-"))
sync_watchlist.py:300:            "source": "sp500",
sync_watchlist.py:318:    parser.add_argument("--no-sp500", action="store_true",
sync_watchlist.py:331:        large_cap = cc.load_sp500()  # S&P 500 = large-cap proxy for biopharma inclusion
sync_watchlist.py:343:    if not args.no_sp500:
sync_watchlist.py:346:        sp_rows, sp_added, sp_unresolved = build_sp500_extras(
sync_watchlist.py:360:    # missing/empty, cohort_for would stamp everything "other"/"sp500" and bake zeros
sync_watchlist.py:371:    missing = [c for c in ("portfolio", "core", "sp500") if not rosters.get(c)]
sync_watchlist.py:374:              f"(portfolio.json/universe_metadata.json) + sigma-alert/sources/sp500.txt are "
sync_watchlist.py:381:                     rosters["core"], rosters["sp500"])
sync_watchlist.py:384:        "core": len(co - rr - p), "sp500": len(sp - co - rr - p),
sync_watchlist.py:399:    n_sp = sum(1 for r in new_rows if r.get("source") == "sp500")
sync_watchlist.py:400:    print(f"By source: cm={n_cm}  sp500={n_sp}")
coverage_cohorts.py:6:    portfolio  ->  researching  ->  core  ->  sp500   (+ `other` residual)
coverage_cohorts.py:9:in the S&P 500 is `portfolio`, never `sp500`). This mirrors the tier partition in
coverage_cohorts.py:17:  - sp500        <- sigma-alert/sources/sp500.txt                 (one ticker/line)
coverage_cohorts.py:30:SP500_FILE = ROOT.parent / "sigma-alert" / "sources" / "sp500.txt"
coverage_cohorts.py:33:COHORT_ORDER = ["portfolio", "researching", "core", "sp500", "other"]
coverage_cohorts.py:39:    "sp500": "S&P 500",
coverage_cohorts.py:79:def load_sp500() -> set[str]:
coverage_cohorts.py:98:        "sp500": load_sp500(),
coverage_cohorts.py:103:    """Return the ticker's cohort by priority (portfolio>researching>core>sp500>other)."""
coverage_cohorts.py:107:    for c in ("portfolio", "researching", "core", "sp500"):
coverage_cohorts.py:122:    """Screening priority: portfolio -> researching -> core -> sp500 -> other, then A-Z."""
data/watchlist.csv:3:ABBV,AbbVie Common Stock,0001551152,general,Large Pharma,,domestic,2026-07-13,,cm,sp500
data/watchlist.csv:4:ABNB,,0001559720,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:5:ACGL,,0000947484,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:6:ACN,,0001467373,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:7:ADBE,Adobe Inc,0000796343,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:9:ADM,,0000007084,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:10:ADP,,0000008670,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:12:AEE,,0001002910,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:13:AEP,,0000004904,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:14:AES,,0000874761,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:15:AFL,,0000004977,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:16:AIG,,0000005272,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:17:AIZ,,0001267238,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:18:AJG,,0000354190,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:19:AKAM,,0001086222,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:20:ALB,,0000915913,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:22:ALL,,0000899051,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:23:ALLE,,0001579241,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:24:AMAT,,0000006951,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:25:AMCR,,0001748790,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:26:AMD,,0000002488,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:27:AME,,0001037868,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:28:AMGN,Amgen Inc,0000318154,general,Large Pharma,,domestic,2026-07-13,,cm,sp500
data/watchlist.csv:29:AMP,,0000820027,general,,Y,domestic,2026-07-13,,sp500,core
data/watchlist.csv:31:AMT,,0001053507,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:33:ANET,,0001596532,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:34:AON,,0000315293,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:35:AOS,,0000091142,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:36:APA,,0001841666,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:37:APD,,0000002969,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:38:APH,,0000820313,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:39:APO,,0001858681,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:40:APP,Applovin Corp,0001751008,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:41:APTV,,0001521332,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:42:ARES,,0001176948,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:45:ATO,,0000731802,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:46:AVB,,0000915912,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:47:AVGO,,0001730168,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:48:AVY,,0000008818,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:49:AWK,,0001410636,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:50:AXON,,0001069183,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:51:AXP,,0000004962,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:52:AZO,,0000866787,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:53:BA,,0000012927,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:54:BAC,,0000070858,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:55:BALL,,0000009389,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:57:BBY,,0000764478,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:58:BEN,,0000038777,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:59:BF-B,,0000014693,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:60:BG,,0001996862,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:61:BIIB,Biogen Inc,0000875045,general,Large Pharma,,domestic,2026-07-13,,cm,sp500
data/watchlist.csv:63:BKNG,,0001075531,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:64:BKR,,0001701605,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:66:BLDR,,0001316835,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:67:BLK,,0002012383,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:69:BMY,Bristol-Myers Squibb Company,0000014272,general,Large Pharma,,domestic,2026-07-13,,cm,sp500
data/watchlist.csv:70:BNY,,0001390777,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:72:BR,,0001383312,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:73:BRK-B,,0001067983,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:75:BX,,0001393818,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:76:BXP,,0001037540,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:77:C,,0000831001,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:78:CARR,,0001783180,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:79:CASY,,0000726958,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:80:CB,,0000896159,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:81:CBOE,,0001374310,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:82:CBRE,,0001138118,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:83:CCI,,0001051470,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:84:CCL,,0000815097,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:85:CDNS,,0000813672,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:86:CDW,,0001402057,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:87:CEG,,0001868275,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:88:CF,,0001324404,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:89:CFG,,0000759944,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:90:CHD,,0000313927,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:92:CHRW,,0001043277,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:93:CHTR,,0001091667,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:94:CIEN,,0000936395,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:95:CINF,,0000020286,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:96:CL,,0000021665,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:97:CLX,,0000021076,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:98:CMCSA,,0001166691,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:99:CME,,0001156375,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:100:CMG,,0001058090,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:101:CMI,,0000026172,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:102:CMS,,0000811156,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:103:CNP,,0001130310,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:104:COF,,0000927628,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:105:COHR,,0000820318,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:106:COP,,0001163165,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:107:COST,,0000909832,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:108:CPAY,,0001175454,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:109:CPT,,0000906345,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:110:CRH,,0000849395,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:111:CRM,"Salesforce, Inc.",0001108524,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:113:CSCO,,0000858877,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:114:CSGP,,0001057352,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:115:CSX,,0000277948,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:116:CTAS,,0000723254,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:117:CTSH,,0001058290,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:118:CTVA,,0001755672,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:119:CVX,,0000093410,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:121:D,,0000715957,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:122:DAL,,0000027904,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:123:DASH,,0001792789,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:125:DD,,0001666700,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:126:DDOG,"Datadog, Inc.",0001561550,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:127:DE,,0000315189,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:128:DECK,,0000910521,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:129:DELL,,0001571996,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:130:DG,,0000029534,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:131:DHI,,0000882184,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:132:DIS,,0001744489,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:133:DLR,,0001297996,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:134:DLTR,,0000935703,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:138:DOV,,0000029905,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:139:DOW,,0001751788,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:140:DPZ,,0001286681,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:141:DRI,,0000940944,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:144:DTE,,0000936340,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:145:DUK,,0001326160,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:147:DVN,,0001090012,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:148:EA,,0000712515,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:149:EBAY,,0001065088,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:150:ECHO,,0001415404,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:151:ECL,,0000031462,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:152:ED,,0001047862,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:153:EFX,,0000033185,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:154:EG,,0001095073,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:155:EIX,,0000827052,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:156:EL,,0001001250,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:157:EME,,0000105634,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:158:EMR,,0000032604,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:159:EOG,,0000821189,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:160:EQIX,,0001101239,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:161:EQR,,0000906107,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:162:EQT,,0000033213,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:163:ERIE,,0000922621,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:164:ES,,0000072741,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:165:ESS,,0000920522,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:166:ETN,,0001551182,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:167:ETR,,0000065984,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:169:EVRG,,0001711269,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:170:EXC,,0001109357,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:171:EXE,,0000895126,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:173:EXPD,,0000746515,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:174:EXPE,,0001324424,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:175:EXR,,0001289490,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:176:F,,0000037996,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:177:FANG,,0001539838,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:178:FAST,,0000815556,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:179:FCX,,0000831259,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:180:FDS,,0001013237,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:181:FE,,0001031296,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:182:FFIV,,0001048695,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:185:FIS,,0001136893,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:186:FISV,,0000798354,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:187:FITB,,0000035527,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:189:FIX,,0001035983,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:190:FLEX,,0000866374,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:191:FOX,,0001754301,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:192:FOXA,,0001754301,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:195:FRT,,0000034903,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:196:FSLR,,0001274494,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:197:FTNT,Fortinet Inc,0001262039,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:198:FTV,,0001659166,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:199:GD,,0000040533,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:200:GDDY,,0001609711,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:201:GE,,0000040545,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:202:GEN,Gen Digital Inc,0000849399,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:203:GEV,,0001996810,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:204:GILD,Gilead Sciences Inc,0000882095,general,Large Pharma,,domestic,2026-07-13,,cm,sp500
data/watchlist.csv:205:GIS,,0000040704,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:206:GL,,0000320335,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:207:GLW,,0000024741,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:208:GM,,0001467858,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:209:GNRC,,0001474735,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:210:GOOG,Alphabet Inc Class C,0001652044,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:211:GOOGL,,0001652044,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:212:GPC,,0000040987,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:213:GPN,,0001123360,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:214:GRMN,,0001121788,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:215:GS,,0000886982,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:217:GWW,,0000277135,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:218:HAL,,0000045012,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:219:HAS,,0000046080,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:220:HBAN,,0000049196,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:221:HD,,0000354950,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:222:HIG,,0000874766,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:223:HII,,0001501585,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:224:HLT,,0001585689,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:225:HON,,0000773840,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:226:HONA,,0002089271,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:227:HPE,,0001645590,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:228:HPQ,,0000047217,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:229:HRL,,0000048465,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:230:HST,,0001070750,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:231:HSY,,0000047111,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:232:HUBB,,0000048898,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:234:HWM,,0000004281,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:235:IBKR,,0001381197,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:236:IBM,,0000051143,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:237:ICE,,0001571949,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:238:IEX,,0000832101,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:239:IFF,,0000051253,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:240:INCY,Incyte Corp,0000879169,general,Biotech,,domestic,2026-07-13,,cm,sp500
data/watchlist.csv:241:INTC,,0000050863,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:242:INTU,,0000896878,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:243:INVH,,0001687229,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:244:IP,,0000051434,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:245:IR,,0001699150,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:246:IRM,,0001020569,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:247:IT,,0000749251,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:248:ITW,,0000049826,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:249:IVZ,,0000914208,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:250:J,,0000052988,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:251:JBHT,,0000728535,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:252:JBL,,0000898293,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:253:JCI,,0000833444,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:254:JKHY,,0000779152,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:255:KDP,,0001418135,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:256:KEY,,0000091576,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:257:KEYS,,0001601046,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:258:KHC,,0001637459,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:259:KIM,,0000879101,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:260:KLAC,,0000319201,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:262:KMB,,0000055785,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:263:KMI,,0001506307,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:264:KO,,0000021344,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:265:KR,,0000056873,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:266:KVUE,,0001944048,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:268:L,,0000060086,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:270:LDOS,,0001336920,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:271:LEN,,0000920760,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:272:LHX,,0000202058,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:273:LII,,0001069202,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:274:LITE,,0001633978,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:276:LMT,,0000936468,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:277:LNT,,0000352541,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:278:LOW,,0000060667,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:280:LRCX,,0000707549,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:281:LUV,,0000092380,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:282:LVS,,0001300514,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:283:LYB,,0001489393,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:284:LYV,,0001335258,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:285:MAA,,0000912595,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:287:MAR,,0001048286,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:288:MAS,,0000062996,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:289:MCD,,0000063908,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:290:MCHP,,0000827054,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:292:MDLZ,,0001103982,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:293:MET,,0001099219,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:294:META,Meta Platforms Inc,0001326801,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:295:MGM,,0000789570,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:296:MKC,,0000063754,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:297:MLM,,0000916076,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:298:MMM,,0000066740,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:300:MNST,,0000865752,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:301:MO,,0000764180,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:302:MOS,,0001285785,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:303:MPC,,0001510295,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:304:MPWR,,0001280452,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:305:MRK,Merck & Co Inc,0000310158,general,Large Pharma,,domestic,2026-07-13,,cm,sp500
data/watchlist.csv:306:MRNA,Moderna Inc,0001682852,general,Biotech,,domestic,2026-07-13,,cm,sp500
data/watchlist.csv:307:MRSH,,0000062709,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:308:MRVL,,0001835632,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:309:MS,,0000895421,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:310:MSCI,,0001408198,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:312:MSI,,0000068505,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:313:MTB,,0000036270,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:314:MU,,0000723125,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:315:NCLH,,0001513761,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:317:NDAQ,,0001120193,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:318:NDSN,,0000072331,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:319:NEE,,0000753308,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:320:NEM,,0001164727,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:323:NI,,0001111711,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:324:NKE,,0000320187,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:325:NOC,,0001133421,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:326:NOW,"ServiceNow, Inc.",0001373715,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:327:NRG,,0001013871,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:328:NSC,,0000702165,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:329:NTAP,,0001002047,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:330:NTRS,,0000073124,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:331:NUE,,0000073309,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:333:NVR,,0000906163,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:334:NWS,,0001564708,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:335:NWSA,,0001564708,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:336:NXPI,,0001413447,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:337:O,,0000726728,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:338:ODFL,,0000878927,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:339:OKE,,0001039684,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:341:OMC,,0000029989,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:342:ON,,0001097864,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:343:ORCL,Oracle Corp,0001341439,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:344:ORLY,,0000898173,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:345:OTIS,,0001781335,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:346:OXY,,0000797468,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:347:PANW,Palo Alto Networks Inc,0001327567,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:349:PAYX,,0000723531,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:350:PCAR,,0000075362,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:351:PCG,,0001004980,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:355:PEG,,0000788784,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:356:PEP,,0000077476,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:357:PFE,Pfizer Inc,0000078003,general,Large Pharma,,domestic,2026-07-13,,cm,sp500
data/watchlist.csv:358:PFG,,0001126328,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:359:PG,,0000080424,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:360:PGR,,0000080661,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:361:PH,,0000076334,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:362:PHM,,0000822416,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:363:PKG,,0000075677,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:364:PLD,,0001045609,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:365:PLTR,,0001321655,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:366:PM,,0001413329,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:367:PNC,,0000713676,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:368:PNR,,0000077360,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:369:PNW,,0000764622,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:370:PPG,,0000079879,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:371:PPL,,0000922224,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:372:PRU,,0001137774,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:373:PSA,,0001393311,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:374:PSKY,,0002041610,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:375:PSX,,0001534701,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:376:PTC,PTC Inc,0000857005,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:377:PWR,,0001050915,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:378:PYPL,,0001633917,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:380:QCOM,,0000804328,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:385:RCL,,0000884887,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:386:REG,,0000910606,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:387:REGN,Regeneron Pharmaceuticals Inc,0000872589,general,Biotech,,domestic,2026-07-13,,cm,sp500
data/watchlist.csv:388:RF,,0001281761,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:389:RJF,,0000720005,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:390:RL,,0001037038,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:392:ROK,,0001024478,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:393:ROL,,0000084839,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:394:ROP,,0000882835,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:395:ROST,,0000745732,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:397:RSG,,0001060391,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:398:RTX,,0000101829,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:402:SBAC,,0001034054,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:403:SBUX,,0000829224,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:404:SCHW,,0000316709,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:406:SHW,,0000089800,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:407:SJM,,0000091419,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:408:SLB,,0000087347,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:409:SMCI,,0001375365,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:411:SNA,,0000091440,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:412:SNDK,,0002023554,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:414:SNPS,,0000883241,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:415:SO,,0000092122,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:416:SPG,,0001063761,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:420:SRE,,0001032208,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:421:STLD,,0001022671,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:422:STT,,0000093751,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:423:STX,,0001137789,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:424:STZ,,0000016918,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:425:SW,,0002005951,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:426:SWK,,0000093556,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:427:SWKS,,0000004127,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:428:SYF,,0001601712,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:429:SYY,,0000096021,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:430:T,,0000732717,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:431:TAP,,0000024545,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:432:TDG,,0001260221,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:433:TDY,,0001094285,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:435:TEL,,0001385157,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:437:TER,,0000097210,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:438:TFC,,0000092230,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:439:TGT,,0000027419,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:440:TJX,,0000109198,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:441:TKO,,0001973266,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:442:TMUS,,0001283699,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:443:TPR,,0001116132,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:445:TRGP,,0001389170,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:446:TRMB,,0000864749,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:447:TROW,,0001113169,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:448:TRV,,0000086312,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:449:TSCO,,0000916365,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:452:TSN,,0000100493,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:453:TT,,0001466258,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:455:TTD,,0001671933,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:456:TTWO,,0000946581,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:458:TXN,,0000097476,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:459:TXT,,0000217346,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:460:TYL,Tyler Technologies Inc,0000860731,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:462:UAL,,0000100517,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:464:UDR,,0000074208,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:465:ULTA,,0001403568,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:466:UNP,,0000100885,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:468:URI,,0001067701,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:469:USB,,0000036104,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:470:VICI,,0001705696,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:471:VLO,,0001035002,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:472:VMC,,0001396009,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:473:VRSK,,0001442145,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:474:VRSN,,0001014473,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:475:VRT,,0001674101,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:476:VRTX,Vertex Pharmaceuticals Inc,0000875320,general,Biotech,,domestic,2026-07-13,,cm,sp500
data/watchlist.csv:477:VST,,0001692819,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:479:VTRS,Viatris Inc,0001792044,general,Specialty & Generic Pharma,,domestic,2026-07-13,,cm,sp500
data/watchlist.csv:480:VZ,,0000732712,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:481:WAB,,0000943452,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:482:WBD,,0001437107,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:483:WDAY,"Workday, Inc.",0001327811,general,,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:484:WDC,,0000106040,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:485:WEC,,0000783325,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:486:WFC,,0000072971,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:488:WM,,0000823768,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:489:WMB,,0000107263,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:490:WRB,,0000011544,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:491:WSM,,0000719955,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:492:WTW,,0001140536,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:493:WY,,0000106535,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:494:WYNN,,0001174922,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:495:XEL,,0000072903,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:496:XOM,,0002115436,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:497:XYL,,0001524472,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:498:XYZ,,0001512673,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:500:YUM,,0001041061,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:501:ZBRA,,0000877212,general,,,domestic,2026-07-13,,sp500,sp500
data/watchlist.csv:515:ARE,Alexandria Real Estate Equities Inc,0001035443,hc_services,Healthcare Real Estate,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:538:DOC,Healthpeak Properties Inc,0000765880,hc_services,Healthcare Real Estate,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:596:VTR,Ventas Inc,0000740260,hc_services,Healthcare Real Estate,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:598:WELL,Welltower Inc,0000766704,hc_services,Healthcare Real Estate,,domestic,2026-04-07,,cm,sp500
data/watchlist.csv:620:BRKR,Bruker Corp,0001109354,medtech,Life Science Tools,Y,domestic,2026-04-07,,cm,core
tests/test_dashboard.py:17:               "core": {"AAA"}, "sp500": {"AAA"}}
tests/test_dashboard.py:23:    rosters = {"portfolio": {"P"}, "researching": {"R"}, "core": {"C"}, "sp500": {"S"}}
tests/test_dashboard.py:26:    assert cc.cohort_for("S", rosters) == "sp500"
tests/test_dashboard.py:31:    rosters = {"portfolio": {"AAA"}, "researching": set(), "core": set(), "sp500": set()}
tests/test_dashboard.py:48:        _wl_row("EEE", "sp500"),                   # pending
tests/test_dashboard.py:55:    totals = {"portfolio": 5, "researching": 0, "core": 4, "sp500": 10}
tests/test_dashboard.py:62:                                                     ("portfolio", "researching", "core", "sp500")})
tests/test_dashboard.py:83:    assert coh["sp500"]["domestic"] == 1
tests/test_dashboard.py:84:    assert coh["sp500"]["pending"] == 1
tests/test_dashboard.py:85:    assert coh["sp500"]["excluded"] == 9
tests/test_dashboard.py:111:        "portfolio": {"XYZ"}, "researching": set(), "core": set(), "sp500": set()})
tests/test_dashboard.py:124:                                                     ("portfolio", "researching", "core", "sp500")})
tests/test_dashboard.py:142:                                                     ("portfolio", "researching", "core", "sp500")})
tests/test_dashboard.py:159:                                                     ("portfolio", "researching", "core", "sp500")})
tests/test_dashboard.py:172:                                                     ("portfolio", "researching", "core", "sp500")})
tests/test_sync_cohorts.py:14:def test_ckey_folds_class_punctuation():
tests/test_sync_cohorts.py:15:    # BRK.B / BRK-B are the same security across sources; GOOG vs GOOGL are not.
tests/test_sync_cohorts.py:16:    assert sw.ckey("BRK.B") == "BRK-B"
tests/test_sync_cohorts.py:17:    assert sw.ckey("brk.b") == "BRK-B"
tests/test_sync_cohorts.py:18:    assert sw.ckey("GOOGL") == "GOOGL"
tests/test_sync_cohorts.py:19:    assert sw.ckey("GOOG") != sw.ckey("GOOGL")
tests/test_sync_cohorts.py:27:    k2 = nb.sort_key({"ticker": "ZZZ", "cohort": "sp500"})
tests/test_sync_cohorts.py:28:    assert k2[0] == cc.COHORT_RANK["sp500"]
tests/test_sync_cohorts.py:29:    assert k < k2  # portfolio sorts before sp500
tests/test_sync_cohorts.py:32:def test_sp500_extras_preserves_cik_on_sec_failure(monkeypatch):
tests/test_sync_cohorts.py:36:        "portfolio": set(), "researching": set(), "core": set(), "sp500": {"ABNB"}})
tests/test_sync_cohorts.py:40:    rows, added, unresolved = sw.build_sp500_extras(
tests/test_sync_cohorts.py:46:def test_sp500_extras_dedup_class_ticker(monkeypatch):
tests/test_sync_cohorts.py:47:    # Codex High: CM carrying BRK-B must suppress re-adding sigma's BRK.B spelling.
tests/test_sync_cohorts.py:49:        "portfolio": set(), "researching": set(), "core": set(), "sp500": {"BRK.B"}})
tests/test_sync_cohorts.py:50:    monkeypatch.setattr(sw, "load_sec_ticker_ciks", lambda: {"BRK-B": "0001067983"})
tests/test_sync_cohorts.py:51:    rows, added, unresolved = sw.build_sp500_extras(
tests/test_sync_cohorts.py:52:        {}, cm_master={"BRK-B"}, already_added=set(), today="2026-07-13")
tests/test_sync_cohorts.py:56:def test_sp500_extras_new_name_without_cik_is_skipped(monkeypatch):
tests/test_sync_cohorts.py:59:        "portfolio": set(), "researching": set(), "core": set(), "sp500": {"NEWCO"}})
tests/test_sync_cohorts.py:61:    rows, added, unresolved = sw.build_sp500_extras(

exec
/bin/bash -lc 'git diff -- next_batch.py run_unattended.py sync_watchlist.py tests/test_sync_cohorts.py' in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
diff --git a/next_batch.py b/next_batch.py
index 0d44eeb..ec29c9a 100644
--- a/next_batch.py
+++ b/next_batch.py
@@ -1,117 +1,117 @@
-"""Pick the next batch of names to screen — the daily "a few each day" driver.
-
-Path B (interactive) chips through the domestic watchlist names a few at a
-time. This deterministic helper picks the next batch so each session knows exactly
-what to screen, prioritised by JP's coverage rings:
-
-  1. by cohort: portfolio -> researching -> core -> sp500 -> other
-  2. then by subgroup within a cohort: hc_services -> medtech -> general (HC focus)
-  3. then alphabetical
-
-A name is "done this cycle" once it has a flags_history row dated >= CYCLE_START
-(the recalibration baseline) WITH status=complete (codex R2 idempotency). A row
-written by a transient fetch failure (status=fetch_failed) does NOT mark the name
-done — it retries next run. A structural Data Gap (foreign/stale/not-disclosed) is
-written status=complete and IS done. Older rows without a status column are treated
-as complete (back-compat). Foreign filers (filer_type=foreign) are never screened by
-EDGAR — they belong to the Data Gap tier — so they're excluded here and reported separately.
-
-Usage:
-  python next_batch.py            # show the next batch (default 8) + progress
-  python next_batch.py -n 5       # batch of 5
-  python next_batch.py --cycle-start 2026-06-20
-"""
-from __future__ import annotations
-
-import argparse
-import csv
-from pathlib import Path
-
-import coverage_cohorts as cc
-
-ROOT = Path(__file__).parent
-WATCHLIST_CSV = ROOT / "data" / "watchlist.csv"
-FLAGS_HISTORY_CSV = ROOT / "data" / "flags_history.csv"
-
-# Names screened on/after this date count as done for the current cycle. Bump it
-# when a fresh full re-screen cycle starts. Default = the 2026-06-20 recalibration.
-DEFAULT_CYCLE_START = "2026-06-20"
-
-SUBGROUP_ORDER = {"hc_services": 0, "medtech": 1, "general": 2}
-
-
-def load_watchlist() -> list[dict]:
-    with WATCHLIST_CSV.open(encoding="utf-8", newline="") as f:
-        return list(csv.DictReader(f))
-
-
-def screened_since(cycle_start: str) -> set[str]:
-    """Tickers DONE this cycle: a flags_history row dated >= cycle_start with status=complete.
-
-    A `status=fetch_failed` row does NOT count as done (transient failure retries next run).
-    Rows without a `status` column (legacy interactive runs) are treated as complete."""
-    if not FLAGS_HISTORY_CSV.exists():
-        return set()
-    done: set[str] = set()
-    with FLAGS_HISTORY_CSV.open(encoding="utf-8", newline="") as f:
-        reader = csv.DictReader(f)
-        has_status = "status" in (reader.fieldnames or [])
-        for row in reader:
-            if (row.get("run_date") or "") < cycle_start or not row.get("ticker"):
-                continue
-            status = (row.get("status") or "").strip() if has_status else "complete"
-            if status in ("", "complete"):
-                done.add(row["ticker"].strip())
-    return done
-
-
-def row_cohort(row: dict, rosters: dict[str, set[str]] | None = None) -> str:
-    """Baked `cohort` column (CI-safe) if present, else compute live from rosters.
-    `rosters=None` → `cohort_for` loads them live (defensive; avoids a hard dep on
-    the caller pre-loading rosters)."""
-    c = (row.get("cohort") or "").strip()
-    return c if c else cc.cohort_for(row.get("ticker", ""), rosters)
-
-
-def sort_key(row: dict, rosters: dict[str, set[str]] | None = None):
-    cohort_rank = cc.COHORT_RANK.get(row_cohort(row, rosters), 99)
-    sg_rank = SUBGROUP_ORDER.get(row.get("sector_subgroup", ""), 9)
-    return (cohort_rank, sg_rank, row.get("ticker", ""))
-
-
-def main() -> int:
-    p = argparse.ArgumentParser(description=__doc__)
-    p.add_argument("-n", "--batch-size", type=int, default=8, help="How many names this batch")
-    p.add_argument("--cycle-start", default=DEFAULT_CYCLE_START,
-                   help=f"Names screened on/after this date count as done (default {DEFAULT_CYCLE_START})")
-    args = p.parse_args()
-
-    watchlist = load_watchlist()
-    rosters = cc.load_rosters()
-    domestic = [r for r in watchlist if r.get("filer_type", "domestic") != "foreign"]
-    foreign = [r for r in watchlist if r.get("filer_type") == "foreign"]
-    done = screened_since(args.cycle_start)
-
-    pending = [r for r in domestic if r["ticker"].strip() not in done]
-    pending.sort(key=lambda r: sort_key(r, rosters))
-
-    n_dom = len(domestic)
-    n_done = sum(1 for r in domestic if r["ticker"].strip() in done)
-    print(f"Cycle start: {args.cycle_start}")
-    print(f"Domestic screened this cycle: {n_done}/{n_dom}  ({n_dom - n_done} pending)")
-    print(f"Foreign (Data Gap, not EDGAR-screened): {len(foreign)}")
-    print()
-    if not pending:
-        print("Cycle complete - every domestic name screened since cycle start. Bump --cycle-start to re-screen.")
-        return 0
-
-    batch = pending[:args.batch_size]
-    print(f"Next batch ({len(batch)}):")
-    for r in batch:
-        cohort = row_cohort(r, rosters)
-        print(f"  {r['ticker']:<6} [{cohort:<11}] {r['sector_subgroup']:<12} {r.get('company_name','')}")
-    return 0
-
-
-if __name__ == "__main__":
-    raise SystemExit(main())
+"""Pick the next batch of names to screen — the daily "a few each day" driver.
+
+Path B (interactive) chips through the domestic watchlist names a few at a
+time. This deterministic helper picks the next batch so each session knows exactly
+what to screen, prioritised by JP's coverage rings:
+
+  1. by cohort: portfolio -> researching -> core -> sp500 -> other
+  2. then by subgroup within a cohort: hc_services -> medtech -> general (HC focus)
+  3. then alphabetical
+
+A name is "done this cycle" once it has a flags_history row dated >= CYCLE_START
+(the recalibration baseline) WITH status=complete (codex R2 idempotency). A row
+written by a transient fetch failure (status=fetch_failed) does NOT mark the name
+done — it retries next run. A structural Data Gap (foreign/stale/not-disclosed) is
+written status=complete and IS done. Older rows without a status column are treated
+as complete (back-compat). Foreign filers (filer_type=foreign) are never screened by
+EDGAR — they belong to the Data Gap tier — so they're excluded here and reported separately.
+
+Usage:
+  python next_batch.py            # show the next batch (default 8) + progress
+  python next_batch.py -n 5       # batch of 5
+  python next_batch.py --cycle-start 2026-06-20
+"""
+from __future__ import annotations
+
+import argparse
+import csv
+from pathlib import Path
+
+import coverage_cohorts as cc
+
+ROOT = Path(__file__).parent
+WATCHLIST_CSV = ROOT / "data" / "watchlist.csv"
+FLAGS_HISTORY_CSV = ROOT / "data" / "flags_history.csv"
+
+# Names screened on/after this date count as done for the current cycle. Bump it
+# when a fresh full re-screen cycle starts. Default = the 2026-06-20 recalibration.
+DEFAULT_CYCLE_START = "2026-06-20"
+
+SUBGROUP_ORDER = {"hc_services": 0, "medtech": 1, "general": 2}
+
+
+def load_watchlist() -> list[dict]:
+    with WATCHLIST_CSV.open(encoding="utf-8", newline="") as f:
+        return list(csv.DictReader(f))
+
+
+def screened_since(cycle_start: str) -> set[str]:
+    """Tickers DONE this cycle: a flags_history row dated >= cycle_start with status=complete.
+
+    A `status=fetch_failed` row does NOT count as done (transient failure retries next run).
+    Rows without a `status` column (legacy interactive runs) are treated as complete."""
+    if not FLAGS_HISTORY_CSV.exists():
+        return set()
+    done: set[str] = set()
+    with FLAGS_HISTORY_CSV.open(encoding="utf-8", newline="") as f:
+        reader = csv.DictReader(f)
+        has_status = "status" in (reader.fieldnames or [])
+        for row in reader:
+            if (row.get("run_date") or "") < cycle_start or not row.get("ticker"):
+                continue
+            status = (row.get("status") or "").strip() if has_status else "complete"
+            if status in ("", "complete"):
+                done.add(row["ticker"].strip())
+    return done
+
+
+def row_cohort(row: dict, rosters: dict[str, set[str]] | None = None) -> str:
+    """Baked `cohort` column (CI-safe) if present, else compute live from rosters.
+    `rosters=None` → `cohort_for` loads them live (defensive; avoids a hard dep on
+    the caller pre-loading rosters)."""
+    c = (row.get("cohort") or "").strip()
+    return c if c else cc.cohort_for(row.get("ticker", ""), rosters)
+
+
+def sort_key(row: dict, rosters: dict[str, set[str]] | None = None):
+    cohort_rank = cc.COHORT_RANK.get(row_cohort(row, rosters), 99)
+    sg_rank = SUBGROUP_ORDER.get(row.get("sector_subgroup", ""), 9)
+    return (cohort_rank, sg_rank, row.get("ticker", ""))
+
+
+def main() -> int:
+    p = argparse.ArgumentParser(description=__doc__)
+    p.add_argument("-n", "--batch-size", type=int, default=8, help="How many names this batch")
+    p.add_argument("--cycle-start", default=DEFAULT_CYCLE_START,
+                   help=f"Names screened on/after this date count as done (default {DEFAULT_CYCLE_START})")
+    args = p.parse_args()
+
+    watchlist = load_watchlist()
+    rosters = cc.load_rosters()
+    domestic = [r for r in watchlist if r.get("filer_type", "domestic") != "foreign"]
+    foreign = [r for r in watchlist if r.get("filer_type") == "foreign"]
+    done = screened_since(args.cycle_start)
+
+    pending = [r for r in domestic if r["ticker"].strip() not in done]
+    pending.sort(key=lambda r: sort_key(r, rosters))
+
+    n_dom = len(domestic)
+    n_done = sum(1 for r in domestic if r["ticker"].strip() in done)
+    print(f"Cycle start: {args.cycle_start}")
+    print(f"Domestic screened this cycle: {n_done}/{n_dom}  ({n_dom - n_done} pending)")
+    print(f"Foreign (Data Gap, not EDGAR-screened): {len(foreign)}")
+    print()
+    if not pending:
+        print("Cycle complete - every domestic name screened since cycle start. Bump --cycle-start to re-screen.")
+        return 0
+
+    batch = pending[:args.batch_size]
+    print(f"Next batch ({len(batch)}):")
+    for r in batch:
+        cohort = row_cohort(r, rosters)
+        print(f"  {r['ticker']:<6} [{cohort:<11}] {r['sector_subgroup']:<12} {r.get('company_name','')}")
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())
diff --git a/sync_watchlist.py b/sync_watchlist.py
index 2c1b575..c9df783 100644
--- a/sync_watchlist.py
+++ b/sync_watchlist.py
@@ -1,439 +1,439 @@
-"""Sync forensic_triage/data/watchlist.csv from Coverage Manager master list.
-
-Reads:  ../Coverage Manager/data/coverage_universe_tickers.csv
-Writes: data/watchlist.csv
-
-Rules:
-  - Only US-listed names (must have a CIK — EDGAR is the data source)
-  - Exclude Biopharma (forensic triage de-prioritizes biotech accounting)
-  - Map Sector (JP) -> forensic subgroup (see SUBGROUP_MAP)
-  - Preserve manual `notes` and original `added_date` for tickers that survive
-  - Report adds / removes / subgroup changes to stdout
-
-Run:  python sync_watchlist.py
-      python sync_watchlist.py --dry-run
-"""
-
-from __future__ import annotations
-
-import argparse
-import csv
-import json
-import os
-import sys
-import urllib.request
-from datetime import date
-from pathlib import Path
-
-ROOT = Path(__file__).parent
-WATCHLIST_CSV = ROOT / "data" / "watchlist.csv"
-
-# SEC ticker->CIK map, used to resolve CIKs for the S&P 500 names that expand the
-# universe beyond CM coverage (the 4th coverage ring). Free, cached locally.
-SEC_TICKERS_URL = "https://www.sec.gov/files/company_tickers.json"
-SEC_TICKERS_CACHE = ROOT / "data" / ".sec_company_tickers.json"
-
-# Coverage CSV path resolution:
-#   1. FORENSIC_COVERAGE_CSV env var (used by remote scheduled trigger)
-#   2. Local sibling layout: ../Coverage Manager/data/coverage_universe_tickers.csv
-def resolve_coverage_csv() -> Path:
-    env_path = os.environ.get("FORENSIC_COVERAGE_CSV")
-    if env_path:
-        return Path(env_path)
-    return ROOT.parent / "Coverage Manager" / "data" / "coverage_universe_tickers.csv"
-
-# Map Coverage Manager Sector (JP) -> forensic_triage sector_subgroup.
-# Kept in sync with Coverage Manager's ALLOWED_SECTORS_JP (config.py).
-# Coverage Manager retired "PA" and "Healthcare Real Estate" on 2026-04-17
-# (collapsed into "Other" and "Healthcare Services" respectively). The new
-# "Healthcare Real Estate" subsector rolls up under hc_services here.
-SUBGROUP_MAP = {
-    "Healthcare Services": "hc_services",
-    "MedTech": "medtech",
-    "Life Science Tools": "medtech",
-    # Large-cap biopharma (S&P 500 only — see LARGE_CAP_ONLY_SECTORS) uses the general
-    # rubric for now (no biopharma-specific rubric yet — a future addition).
-    "Biopharma": "general",
-    # Everything else that survives the filter falls through to "general"
-    "Tech": "general",
-    "SaaS": "general",
-    "Fintech": "general",
-    "Other": "general",
-}
-
-EXCLUDED_SECTORS: set[str] = set()  # (was {"Biopharma"} — now conditional, see below)
-
-# Biopharma is screened ONLY when large-cap (proxied by S&P 500 membership): big pharma
-# (LLY/PFE/MRK…) has normal financials the rubric handles; small/pre-revenue biotech
-# accruals/revenue rules are noise, so non-S&P-500 biopharma stays excluded. (JP 2026-07-13.)
-LARGE_CAP_ONLY_SECTORS = {"Biopharma"}
-
-WATCHLIST_FIELDS = [
-    "ticker",
-    "company_name",
-    "cik",
-    "sector_subgroup",
-    "subsector",
-    "core",
-    "filer_type",
-    "added_date",
-    "notes",
-    "source",  # "cm" (Coverage Manager universe) or "sp500" (S&P 500 expansion ring)
-    "cohort",  # portfolio|researching|core|sp500|other — baked so CI (no sibling repos) reads it
-]
-
-COHORT_TOTALS_JSON = ROOT / "data" / "cohort_totals.json"
-
-
-def ckey(t: str) -> str:
-    """Canonical ticker key for cross-source comparison. SEC / sigma-S&P / CM disagree
-    on class-share punctuation (`BRK.B` vs `BRK-B`, `BF.B` vs `BF-B`); fold `.`→`-` so
-    the same security matches across sources and isn't double-added. Distinct tickers
-    like GOOG vs GOOGL are unaffected (different letters, not punctuation)."""
-    return (t or "").strip().upper().replace(".", "-")
-
-
-def normalize_cik(raw: str) -> str:
-    """Normalize a Coverage Manager CIK to a zero-padded 10-digit string.
-
-    EDGAR APIs want a 10-digit zero-padded CIK. CM stores it variously
-    (with/without leading zeros, sometimes a 'CIK' prefix). Empty -> ''.
-    """
-    s = (raw or "").strip()
-    if not s:
-        return ""
-    s = s.upper().removeprefix("CIK").lstrip("0") or "0"
-    if not s.isdigit():
-        return ""
-    return s.zfill(10)
-
-
-def derive_filer_type(row: dict) -> str:
-    """domestic (10-K filer) vs foreign (20-F/IFRS filer).
-
-    The forensic rubric is built on 10-K disclosures (financial-statement notes,
-    MD&A). ADR / cross-listed foreign private issuers file a 20-F with different
-    structure and often IFRS, which the 10-K-based rubric cannot evaluate
-    cleanly — so they must be routed to manual review (Data Gap tier), NOT
-    silently screened as Green. We TAG rather than drop so coverage stays visible.
-    """
-    iso = (row.get("Country (ISO)", "") or "").strip().upper()
-    listing = (row.get("Listing Type", "") or "").strip().lower()
-    if (iso and iso != "USA") or "adr" in listing or "cross-listed" in listing:
-        return "foreign"
-    return "domestic"
-
-
-def load_existing_watchlist() -> dict[str, dict]:
-    if not WATCHLIST_CSV.exists():
-        return {}
-    with WATCHLIST_CSV.open(encoding="utf-8", newline="") as f:
-        reader = csv.DictReader(f)
-        return {row["ticker"]: row for row in reader if row.get("ticker")}
-
-
-def load_coverage(coverage_csv: Path, large_cap: set[str] | None = None) -> list[dict]:
-    large_cap = large_cap or set()
-    rows = []
-    with coverage_csv.open(encoding="utf-8", newline="") as f:
-        reader = csv.DictReader(f)
-        for row in reader:
-            if not row.get("CIK"):
-                continue
-            sector = row.get("Sector (JP)", "").strip()
-            if sector in EXCLUDED_SECTORS:
-                continue
-            if sector not in SUBGROUP_MAP:
-                continue
-            # Biopharma only if large-cap (S&P 500) — see LARGE_CAP_ONLY_SECTORS.
-            if sector in LARGE_CAP_ONLY_SECTORS:
-                ticker = (row.get("Ticker", "") or "").strip().upper()
-                if ticker not in large_cap:
-                    continue
-            rows.append(row)
-    return rows
-
-
-def build_new_watchlist(
-    coverage_rows: list[dict],
-    existing: dict[str, dict],
-    today: str,
-) -> tuple[list[dict], list[str], list[str], list[tuple[str, str, str]]]:
-    """Returns (new_rows, added_tickers, removed_tickers, subgroup_changes)."""
-    new_rows: list[dict] = []
-    seen: set[str] = set()
-    added: list[str] = []
-    subgroup_changes: list[tuple[str, str, str]] = []
-
-    for row in coverage_rows:
-        ticker = row["Ticker"].strip()
-        if not ticker or ticker in seen:
-            continue
-        seen.add(ticker)
-
-        sector = row["Sector (JP)"].strip()
-        subgroup = SUBGROUP_MAP[sector]
-        prev = existing.get(ticker)
-
-        new_row = {
-            "ticker": ticker,
-            "company_name": row.get("Company Name", "").strip(),
-            "cik": normalize_cik(row.get("CIK", "")),
-            "sector_subgroup": subgroup,
-            "subsector": row.get("Subsector (JP)", "").strip(),
-            "core": row.get("Core", "").strip(),
-            "filer_type": derive_filer_type(row),
-            "added_date": prev["added_date"] if prev and prev.get("added_date") else today,
-            "notes": prev.get("notes", "") if prev else "",
-            "source": "cm",
-        }
-        new_rows.append(new_row)
-
-        if not prev:
-            added.append(ticker)
-        elif prev.get("sector_subgroup") and prev["sector_subgroup"] != subgroup:
-            subgroup_changes.append((ticker, prev["sector_subgroup"], subgroup))
-
-    removed = sorted(set(existing) - seen)
-    new_rows.sort(key=lambda r: (r["sector_subgroup"], r["ticker"]))
-    return new_rows, sorted(added), removed, subgroup_changes
-
-
-def load_sec_ticker_ciks() -> dict[str, str]:
-    """Return {TICKER: zero-padded-10-digit CIK} from SEC's ticker map.
-
-    Cached at SEC_TICKERS_CACHE; fetched once (free, no key — just a descriptive
-    User-Agent per SEC policy). On any fetch failure with no cache, returns {} and
-    the extras step degrades loudly (names without a CIK can't be EDGAR-screened).
-    """
-    data = None
-    if SEC_TICKERS_CACHE.exists():
-        try:
-            data = json.loads(SEC_TICKERS_CACHE.read_text(encoding="utf-8"))
-        except Exception:
-            data = None
-    if data is None:
-        ua = os.environ.get("EDGAR_IDENTITY") or "Jason Peterson jroypeterson@gmail.com"
-        req = urllib.request.Request(SEC_TICKERS_URL, headers={"User-Agent": ua})
-        try:
-            with urllib.request.urlopen(req, timeout=30) as resp:
-                data = json.loads(resp.read().decode("utf-8"))
-            SEC_TICKERS_CACHE.write_text(json.dumps(data), encoding="utf-8")
-        except Exception as e:
-            print(f"WARNING: could not fetch SEC ticker map ({e}); "
-                  f"S&P 500 extras without a cached CIK will be skipped.", file=sys.stderr)
-            return {}
-    out: dict[str, str] = {}
-    for rec in data.values():
-        t = str(rec.get("ticker", "")).strip().upper()
-        cik = rec.get("cik_str")
-        if t and cik is not None:
-            out[t] = str(cik).zfill(10)
-    return out
-
-
-def load_cm_master_tickers(coverage_csv: Path) -> set[str]:
-    """Every ticker in the CM master (pre-filter), so the S&P 500 expansion only
-    ADDS names CM doesn't already carry — it never re-introduces a name CM
-    deliberately filtered out (e.g. a Biopharma name)."""
-    out: set[str] = set()
-    with coverage_csv.open(encoding="utf-8", newline="") as f:
-        for row in csv.DictReader(f):
-            t = (row.get("Ticker", "") or "").strip().upper()
-            if t:
-                out.add(t)
-    return out
-
-
-def build_sp500_extras(
-    existing: dict[str, dict],
-    cm_master: set[str],
-    already_added: set[str],
-    today: str,
-) -> tuple[list[dict], list[str], list[str]]:
-    """Rows for the S&P 500 expansion ring: roster-priority names NOT in the CM
-    master. Returns (rows, added_tickers, unresolved_no_cik)."""
-    try:
-        import coverage_cohorts as cc
-    except Exception as e:
-        print(f"WARNING: coverage_cohorts unavailable ({e}); skipping S&P 500 expansion.",
-              file=sys.stderr)
-        return [], [], []
-
-    rosters = cc.load_rosters()
-    # The expansion ring is the S&P 500 specifically. Portfolio/Researching/Core are
-    # already CM data (in cm_master); only the S&P 500 adds names CM doesn't carry.
-    # (Using just the sp500 roster avoids pulling in foreign CM-position tickers whose
-    # export format differs from the CM master, which have no SEC CIK anyway.)
-    core_set = rosters["core"]
-    # Compare on the canonical key so a class-ticker CM already carries (BRK-B) isn't
-    # re-added under sigma's spelling (BRK.B).
-    cm_canon = {ckey(t) for t in cm_master}
-    added_canon = {ckey(t) for t in already_added}
-    extras = sorted(t for t in rosters["sp500"]
-                    if ckey(t) not in cm_canon and ckey(t) not in added_canon)
-
-    sec_ciks = load_sec_ticker_ciks() if extras else {}
-    rows: list[dict] = []
-    added: list[str] = []
-    unresolved: list[str] = []
-    for ticker in extras:
-        prev = existing.get(ticker)
-        # Prefer a fresh SEC resolution, but fall back to the already-known CIK on an
-        # existing row so a transient SEC-map outage can't DROP established S&P rows
-        # (which would then be reported "removed" and written out of the watchlist).
-        cik = (sec_ciks.get(ticker) or sec_ciks.get(ticker.replace(".", "-"))
-               or (prev.get("cik") if prev and prev.get("cik") else ""))
-        if not cik:
-            unresolved.append(ticker)
-            continue  # no CIK -> can't EDGAR-screen; skip loudly (reported below)
-        rows.append({
-            "ticker": ticker,
-            "company_name": prev.get("company_name", "") if prev else "",
-            "cik": cik,
-            "sector_subgroup": "general",
-            "subsector": "",
-            "core": "Y" if ticker in core_set else "",
-            "filer_type": "domestic",  # S&P 500 constituents are US 10-K filers
-            "added_date": prev["added_date"] if prev and prev.get("added_date") else today,
-            "notes": prev.get("notes", "") if prev else "",
-            "source": "sp500",
-        })
-        if not prev:
-            added.append(ticker)
-    return rows, sorted(added), sorted(unresolved)
-
-
-def write_watchlist(rows: list[dict]) -> None:
-    WATCHLIST_CSV.parent.mkdir(parents=True, exist_ok=True)
-    with WATCHLIST_CSV.open("w", encoding="utf-8", newline="") as f:
-        writer = csv.DictWriter(f, fieldnames=WATCHLIST_FIELDS)
-        writer.writeheader()
-        writer.writerows(rows)
-
-
-def main() -> int:
-    parser = argparse.ArgumentParser(description=__doc__)
-    parser.add_argument("--dry-run", action="store_true", help="Report changes without writing")
-    parser.add_argument("--no-sp500", action="store_true",
-                        help="Skip the S&P 500 expansion ring (CM universe only)")
-    args = parser.parse_args()
-
-    coverage_csv = resolve_coverage_csv()
-    if not coverage_csv.exists():
-        print(f"ERROR: coverage CSV not found at {coverage_csv}", file=sys.stderr)
-        print("Set FORENSIC_COVERAGE_CSV env var or check sibling-folder layout.", file=sys.stderr)
-        return 1
-
-    existing = load_existing_watchlist()
-    try:
-        import coverage_cohorts as cc
-        large_cap = cc.load_sp500()  # S&P 500 = large-cap proxy for biopharma inclusion
-    except Exception:
-        large_cap = set()
-    coverage_rows = load_coverage(coverage_csv, large_cap)
-    today = date.today().isoformat()
-    new_rows, added, removed, subgroup_changes = build_new_watchlist(
-        coverage_rows, existing, today
-    )
-
-    # S&P 500 expansion ring: add roster names CM doesn't already carry.
-    sp_added: list[str] = []
-    sp_unresolved: list[str] = []
-    if not args.no_sp500:
-        cm_master = load_cm_master_tickers(coverage_csv)
-        already = {r["ticker"] for r in new_rows}
-        sp_rows, sp_added, sp_unresolved = build_sp500_extras(
-            existing, cm_master, already, today
-        )
-        new_rows.extend(sp_rows)
-        new_rows.sort(key=lambda r: (r["sector_subgroup"], r["ticker"]))
-
-    # Recompute removals against the FINAL set (build_new_watchlist only saw the CM
-    # pass, so without this the S&P 500 rows the extras step re-adds show as removed).
-    final_tickers = {r["ticker"] for r in new_rows}
-    removed = sorted(set(existing) - final_tickers)
-
-    # Bake the cohort onto every row (+ disjoint roster totals) so CI — which has no
-    # CM/sigma sibling repos — reads cohorts from the committed watchlist, not live.
-    # The rosters are REQUIRED for a correct local sync: if the sibling exports are
-    # missing/empty, cohort_for would stamp everything "other"/"sp500" and bake zeros
-    # into cohort_totals.json — poisoned columns CI then trusts. So abort the write
-    # rather than persist a mis-partitioned universe.
-    cohort_totals: dict[str, int] = {}
-    try:
-        import coverage_cohorts as cc
-        rosters = cc.load_rosters()
-    except Exception as e:
-        print(f"ERROR: coverage_cohorts unavailable ({e}); cannot bake cohorts — refusing "
-              f"to write a watchlist without them.", file=sys.stderr)
-        return 1
-    missing = [c for c in ("portfolio", "core", "sp500") if not rosters.get(c)]
-    if missing:
-        print(f"ERROR: coverage rosters empty/missing: {', '.join(missing)}. The CM exports "
-              f"(portfolio.json/universe_metadata.json) + sigma-alert/sources/sp500.txt are "
-              f"REQUIRED for a correct local sync. Refusing to bake poisoned cohorts into "
-              f"watchlist.csv — check the sibling repos.", file=sys.stderr)
-        return 1
-    for r in new_rows:
-        r["cohort"] = cc.cohort_for(r["ticker"], rosters)
-    p, rr, co, sp = (rosters["portfolio"], rosters["researching"],
-                     rosters["core"], rosters["sp500"])
-    cohort_totals = {
-        "portfolio": len(p), "researching": len(rr - p),
-        "core": len(co - rr - p), "sp500": len(sp - co - rr - p),
-    }
-
-    by_subgroup: dict[str, int] = {}
-    for r in new_rows:
-        by_subgroup[r["sector_subgroup"]] = by_subgroup.get(r["sector_subgroup"], 0) + 1
-
-    print(f"Coverage source: {coverage_csv}")
-    print(f"Watchlist target: {WATCHLIST_CSV}")
-    print(f"Total in new watchlist: {len(new_rows)}")
-    for sg in sorted(by_subgroup):
-        print(f"  {sg}: {by_subgroup[sg]}")
-    foreign = sum(1 for r in new_rows if r["filer_type"] == "foreign")
-    print(f"Foreign (20-F/ADR -> Data Gap manual review): {foreign} of {len(new_rows)}")
-    n_cm = sum(1 for r in new_rows if r.get("source") == "cm")
-    n_sp = sum(1 for r in new_rows if r.get("source") == "sp500")
-    print(f"By source: cm={n_cm}  sp500={n_sp}")
-    print(f"Added (CM): {len(added)}")
-    print(f"Added (S&P 500 ring): {len(sp_added)}")
-    if sp_unresolved:
-        print(f"S&P 500 names skipped (no CIK in SEC map): {len(sp_unresolved)} "
-              f"-> {', '.join(sp_unresolved[:15])}{' ...' if len(sp_unresolved) > 15 else ''}")
-    print(f"Removed: {len(removed)}")
-    print(f"Subgroup changes: {len(subgroup_changes)}")
-
-    if added:
-        print("\n+ Added:")
-        for t in added[:50]:
-            print(f"  + {t}")
-        if len(added) > 50:
-            print(f"  ... and {len(added) - 50} more")
-    if removed:
-        print("\n- Removed:")
-        for t in removed[:50]:
-            print(f"  - {t}")
-        if len(removed) > 50:
-            print(f"  ... and {len(removed) - 50} more")
-    if subgroup_changes:
-        print("\n~ Subgroup changes:")
-        for t, old, new in subgroup_changes[:50]:
-            print(f"  ~ {t}: {old} -> {new}")
-
-    if args.dry_run:
-        print("\n[dry-run] no file written")
-        return 0
-
-    write_watchlist(new_rows)
-    print(f"\nWrote {len(new_rows)} rows to {WATCHLIST_CSV}")
-    if cohort_totals:
-        COHORT_TOTALS_JSON.write_text(json.dumps(cohort_totals, indent=2), encoding="utf-8")
-        print(f"Wrote cohort totals to {COHORT_TOTALS_JSON}: {cohort_totals}")
-    return 0
-
-
-if __name__ == "__main__":
-    raise SystemExit(main())
+"""Sync forensic_triage/data/watchlist.csv from Coverage Manager master list.
+
+Reads:  ../Coverage Manager/data/coverage_universe_tickers.csv
+Writes: data/watchlist.csv
+
+Rules:
+  - Only US-listed names (must have a CIK — EDGAR is the data source)
+  - Exclude Biopharma (forensic triage de-prioritizes biotech accounting)
+  - Map Sector (JP) -> forensic subgroup (see SUBGROUP_MAP)
+  - Preserve manual `notes` and original `added_date` for tickers that survive
+  - Report adds / removes / subgroup changes to stdout
+
+Run:  python sync_watchlist.py
+      python sync_watchlist.py --dry-run
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import json
+import os
+import sys
+import urllib.request
+from datetime import date
+from pathlib import Path
+
+ROOT = Path(__file__).parent
+WATCHLIST_CSV = ROOT / "data" / "watchlist.csv"
+
+# SEC ticker->CIK map, used to resolve CIKs for the S&P 500 names that expand the
+# universe beyond CM coverage (the 4th coverage ring). Free, cached locally.
+SEC_TICKERS_URL = "https://www.sec.gov/files/company_tickers.json"
+SEC_TICKERS_CACHE = ROOT / "data" / ".sec_company_tickers.json"
+
+# Coverage CSV path resolution:
+#   1. FORENSIC_COVERAGE_CSV env var (used by remote scheduled trigger)
+#   2. Local sibling layout: ../Coverage Manager/data/coverage_universe_tickers.csv
+def resolve_coverage_csv() -> Path:
+    env_path = os.environ.get("FORENSIC_COVERAGE_CSV")
+    if env_path:
+        return Path(env_path)
+    return ROOT.parent / "Coverage Manager" / "data" / "coverage_universe_tickers.csv"
+
+# Map Coverage Manager Sector (JP) -> forensic_triage sector_subgroup.
+# Kept in sync with Coverage Manager's ALLOWED_SECTORS_JP (config.py).
+# Coverage Manager retired "PA" and "Healthcare Real Estate" on 2026-04-17
+# (collapsed into "Other" and "Healthcare Services" respectively). The new
+# "Healthcare Real Estate" subsector rolls up under hc_services here.
+SUBGROUP_MAP = {
+    "Healthcare Services": "hc_services",
+    "MedTech": "medtech",
+    "Life Science Tools": "medtech",
+    # Large-cap biopharma (S&P 500 only — see LARGE_CAP_ONLY_SECTORS) uses the general
+    # rubric for now (no biopharma-specific rubric yet — a future addition).
+    "Biopharma": "general",
+    # Everything else that survives the filter falls through to "general"
+    "Tech": "general",
+    "SaaS": "general",
+    "Fintech": "general",
+    "Other": "general",
+}
+
+EXCLUDED_SECTORS: set[str] = set()  # (was {"Biopharma"} — now conditional, see below)
+
+# Biopharma is screened ONLY when large-cap (proxied by S&P 500 membership): big pharma
+# (LLY/PFE/MRK…) has normal financials the rubric handles; small/pre-revenue biotech
+# accruals/revenue rules are noise, so non-S&P-500 biopharma stays excluded. (JP 2026-07-13.)
+LARGE_CAP_ONLY_SECTORS = {"Biopharma"}
+
+WATCHLIST_FIELDS = [
+    "ticker",
+    "company_name",
+    "cik",
+    "sector_subgroup",
+    "subsector",
+    "core",
+    "filer_type",
+    "added_date",
+    "notes",
+    "source",  # "cm" (Coverage Manager universe) or "sp500" (S&P 500 expansion ring)
+    "cohort",  # portfolio|researching|core|sp500|other — baked so CI (no sibling repos) reads it
+]
+
+COHORT_TOTALS_JSON = ROOT / "data" / "cohort_totals.json"
+
+
+def ckey(t: str) -> str:
+    """Canonical ticker key for cross-source comparison. SEC / sigma-S&P / CM disagree
+    on class-share punctuation (`BRK.B` vs `BRK-B`, `BF.B` vs `BF-B`); fold `.`→`-` so
+    the same security matches across sources and isn't double-added. Distinct tickers
+    like GOOG vs GOOGL are unaffected (different letters, not punctuation)."""
+    return (t or "").strip().upper().replace(".", "-")
+
+
+def normalize_cik(raw: str) -> str:
+    """Normalize a Coverage Manager CIK to a zero-padded 10-digit string.
+
+    EDGAR APIs want a 10-digit zero-padded CIK. CM stores it variously
+    (with/without leading zeros, sometimes a 'CIK' prefix). Empty -> ''.
+    """
+    s = (raw or "").strip()
+    if not s:
+        return ""
+    s = s.upper().removeprefix("CIK").lstrip("0") or "0"
+    if not s.isdigit():
+        return ""
+    return s.zfill(10)
+
+
+def derive_filer_type(row: dict) -> str:
+    """domestic (10-K filer) vs foreign (20-F/IFRS filer).
+
+    The forensic rubric is built on 10-K disclosures (financial-statement notes,
+    MD&A). ADR / cross-listed foreign private issuers file a 20-F with different
+    structure and often IFRS, which the 10-K-based rubric cannot evaluate
+    cleanly — so they must be routed to manual review (Data Gap tier), NOT
+    silently screened as Green. We TAG rather than drop so coverage stays visible.
+    """
+    iso = (row.get("Country (ISO)", "") or "").strip().upper()
+    listing = (row.get("Listing Type", "") or "").strip().lower()
+    if (iso and iso != "USA") or "adr" in listing or "cross-listed" in listing:
+        return "foreign"
+    return "domestic"
+
+
+def load_existing_watchlist() -> dict[str, dict]:
+    if not WATCHLIST_CSV.exists():
+        return {}
+    with WATCHLIST_CSV.open(encoding="utf-8", newline="") as f:
+        reader = csv.DictReader(f)
+        return {row["ticker"]: row for row in reader if row.get("ticker")}
+
+
+def load_coverage(coverage_csv: Path, large_cap: set[str] | None = None) -> list[dict]:
+    large_cap = large_cap or set()
+    rows = []
+    with coverage_csv.open(encoding="utf-8", newline="") as f:
+        reader = csv.DictReader(f)
+        for row in reader:
+            if not row.get("CIK"):
+                continue
+            sector = row.get("Sector (JP)", "").strip()
+            if sector in EXCLUDED_SECTORS:
+                continue
+            if sector not in SUBGROUP_MAP:
+                continue
+            # Biopharma only if large-cap (S&P 500) — see LARGE_CAP_ONLY_SECTORS.
+            if sector in LARGE_CAP_ONLY_SECTORS:
+                ticker = (row.get("Ticker", "") or "").strip().upper()
+                if ticker not in large_cap:
+                    continue
+            rows.append(row)
+    return rows
+
+
+def build_new_watchlist(
+    coverage_rows: list[dict],
+    existing: dict[str, dict],
+    today: str,
+) -> tuple[list[dict], list[str], list[str], list[tuple[str, str, str]]]:
+    """Returns (new_rows, added_tickers, removed_tickers, subgroup_changes)."""
+    new_rows: list[dict] = []
+    seen: set[str] = set()
+    added: list[str] = []
+    subgroup_changes: list[tuple[str, str, str]] = []
+
+    for row in coverage_rows:
+        ticker = row["Ticker"].strip()
+        if not ticker or ticker in seen:
+            continue
+        seen.add(ticker)
+
+        sector = row["Sector (JP)"].strip()
+        subgroup = SUBGROUP_MAP[sector]
+        prev = existing.get(ticker)
+
+        new_row = {
+            "ticker": ticker,
+            "company_name": row.get("Company Name", "").strip(),
+            "cik": normalize_cik(row.get("CIK", "")),
+            "sector_subgroup": subgroup,
+            "subsector": row.get("Subsector (JP)", "").strip(),
+            "core": row.get("Core", "").strip(),
+            "filer_type": derive_filer_type(row),
+            "added_date": prev["added_date"] if prev and prev.get("added_date") else today,
+            "notes": prev.get("notes", "") if prev else "",
+            "source": "cm",
+        }
+        new_rows.append(new_row)
+
+        if not prev:
+            added.append(ticker)
+        elif prev.get("sector_subgroup") and prev["sector_subgroup"] != subgroup:
+            subgroup_changes.append((ticker, prev["sector_subgroup"], subgroup))
+
+    removed = sorted(set(existing) - seen)
+    new_rows.sort(key=lambda r: (r["sector_subgroup"], r["ticker"]))
+    return new_rows, sorted(added), removed, subgroup_changes
+
+
+def load_sec_ticker_ciks() -> dict[str, str]:
+    """Return {TICKER: zero-padded-10-digit CIK} from SEC's ticker map.
+
+    Cached at SEC_TICKERS_CACHE; fetched once (free, no key — just a descriptive
+    User-Agent per SEC policy). On any fetch failure with no cache, returns {} and
+    the extras step degrades loudly (names without a CIK can't be EDGAR-screened).
+    """
+    data = None
+    if SEC_TICKERS_CACHE.exists():
+        try:
+            data = json.loads(SEC_TICKERS_CACHE.read_text(encoding="utf-8"))
+        except Exception:
+            data = None
+    if data is None:
+        ua = os.environ.get("EDGAR_IDENTITY") or "Jason Peterson jroypeterson@gmail.com"
+        req = urllib.request.Request(SEC_TICKERS_URL, headers={"User-Agent": ua})
+        try:
+            with urllib.request.urlopen(req, timeout=30) as resp:
+                data = json.loads(resp.read().decode("utf-8"))
+            SEC_TICKERS_CACHE.write_text(json.dumps(data), encoding="utf-8")
+        except Exception as e:
+            print(f"WARNING: could not fetch SEC ticker map ({e}); "
+                  f"S&P 500 extras without a cached CIK will be skipped.", file=sys.stderr)
+            return {}
+    out: dict[str, str] = {}
+    for rec in data.values():
+        t = str(rec.get("ticker", "")).strip().upper()
+        cik = rec.get("cik_str")
+        if t and cik is not None:
+            out[t] = str(cik).zfill(10)
+    return out
+
+
+def load_cm_master_tickers(coverage_csv: Path) -> set[str]:
+    """Every ticker in the CM master (pre-filter), so the S&P 500 expansion only
+    ADDS names CM doesn't already carry — it never re-introduces a name CM
+    deliberately filtered out (e.g. a Biopharma name)."""
+    out: set[str] = set()
+    with coverage_csv.open(encoding="utf-8", newline="") as f:
+        for row in csv.DictReader(f):
+            t = (row.get("Ticker", "") or "").strip().upper()
+            if t:
+                out.add(t)
+    return out
+
+
+def build_sp500_extras(
+    existing: dict[str, dict],
+    cm_master: set[str],
+    already_added: set[str],
+    today: str,
+) -> tuple[list[dict], list[str], list[str]]:
+    """Rows for the S&P 500 expansion ring: roster-priority names NOT in the CM
+    master. Returns (rows, added_tickers, unresolved_no_cik)."""
+    try:
+        import coverage_cohorts as cc
+    except Exception as e:
+        print(f"WARNING: coverage_cohorts unavailable ({e}); skipping S&P 500 expansion.",
+              file=sys.stderr)
+        return [], [], []
+
+    rosters = cc.load_rosters()
+    # The expansion ring is the S&P 500 specifically. Portfolio/Researching/Core are
+    # already CM data (in cm_master); only the S&P 500 adds names CM doesn't carry.
+    # (Using just the sp500 roster avoids pulling in foreign CM-position tickers whose
+    # export format differs from the CM master, which have no SEC CIK anyway.)
+    core_set = rosters["core"]
+    # Compare on the canonical key so a class-ticker CM already carries (BRK-B) isn't
+    # re-added under sigma's spelling (BRK.B).
+    cm_canon = {ckey(t) for t in cm_master}
+    added_canon = {ckey(t) for t in already_added}
+    extras = sorted(t for t in rosters["sp500"]
+                    if ckey(t) not in cm_canon and ckey(t) not in added_canon)
+
+    sec_ciks = load_sec_ticker_ciks() if extras else {}
+    rows: list[dict] = []
+    added: list[str] = []
+    unresolved: list[str] = []
+    for ticker in extras:
+        prev = existing.get(ticker)
+        # Prefer a fresh SEC resolution, but fall back to the already-known CIK on an
+        # existing row so a transient SEC-map outage can't DROP established S&P rows
+        # (which would then be reported "removed" and written out of the watchlist).
+        cik = (sec_ciks.get(ticker) or sec_ciks.get(ticker.replace(".", "-"))
+               or (prev.get("cik") if prev and prev.get("cik") else ""))
+        if not cik:
+            unresolved.append(ticker)
+            continue  # no CIK -> can't EDGAR-screen; skip loudly (reported below)
+        rows.append({
+            "ticker": ticker,
+            "company_name": prev.get("company_name", "") if prev else "",
+            "cik": cik,
+            "sector_subgroup": "general",
+            "subsector": "",
+            "core": "Y" if ticker in core_set else "",
+            "filer_type": "domestic",  # S&P 500 constituents are US 10-K filers
+            "added_date": prev["added_date"] if prev and prev.get("added_date") else today,
+            "notes": prev.get("notes", "") if prev else "",
+            "source": "sp500",
+        })
+        if not prev:
+            added.append(ticker)
+    return rows, sorted(added), sorted(unresolved)
+
+
+def write_watchlist(rows: list[dict]) -> None:
+    WATCHLIST_CSV.parent.mkdir(parents=True, exist_ok=True)
+    with WATCHLIST_CSV.open("w", encoding="utf-8", newline="") as f:
+        writer = csv.DictWriter(f, fieldnames=WATCHLIST_FIELDS)
+        writer.writeheader()
+        writer.writerows(rows)
+
+
+def main() -> int:
+    parser = argparse.ArgumentParser(description=__doc__)
+    parser.add_argument("--dry-run", action="store_true", help="Report changes without writing")
+    parser.add_argument("--no-sp500", action="store_true",
+                        help="Skip the S&P 500 expansion ring (CM universe only)")
+    args = parser.parse_args()
+
+    coverage_csv = resolve_coverage_csv()
+    if not coverage_csv.exists():
+        print(f"ERROR: coverage CSV not found at {coverage_csv}", file=sys.stderr)
+        print("Set FORENSIC_COVERAGE_CSV env var or check sibling-folder layout.", file=sys.stderr)
+        return 1
+
+    existing = load_existing_watchlist()
+    try:
+        import coverage_cohorts as cc
+        large_cap = cc.load_sp500()  # S&P 500 = large-cap proxy for biopharma inclusion
+    except Exception:
+        large_cap = set()
+    coverage_rows = load_coverage(coverage_csv, large_cap)
+    today = date.today().isoformat()
+    new_rows, added, removed, subgroup_changes = build_new_watchlist(
+        coverage_rows, existing, today
+    )
+
+    # S&P 500 expansion ring: add roster names CM doesn't already carry.
+    sp_added: list[str] = []
+    sp_unresolved: list[str] = []
+    if not args.no_sp500:
+        cm_master = load_cm_master_tickers(coverage_csv)
+        already = {r["ticker"] for r in new_rows}
+        sp_rows, sp_added, sp_unresolved = build_sp500_extras(
+            existing, cm_master, already, today
+        )
+        new_rows.extend(sp_rows)
+        new_rows.sort(key=lambda r: (r["sector_subgroup"], r["ticker"]))
+
+    # Recompute removals against the FINAL set (build_new_watchlist only saw the CM
+    # pass, so without this the S&P 500 rows the extras step re-adds show as removed).
+    final_tickers = {r["ticker"] for r in new_rows}
+    removed = sorted(set(existing) - final_tickers)
+
+    # Bake the cohort onto every row (+ disjoint roster totals) so CI — which has no
+    # CM/sigma sibling repos — reads cohorts from the committed watchlist, not live.
+    # The rosters are REQUIRED for a correct local sync: if the sibling exports are
+    # missing/empty, cohort_for would stamp everything "other"/"sp500" and bake zeros
+    # into cohort_totals.json — poisoned columns CI then trusts. So abort the write
+    # rather than persist a mis-partitioned universe.
+    cohort_totals: dict[str, int] = {}
+    try:
+        import coverage_cohorts as cc
+        rosters = cc.load_rosters()
+    except Exception as e:
+        print(f"ERROR: coverage_cohorts unavailable ({e}); cannot bake cohorts — refusing "
+              f"to write a watchlist without them.", file=sys.stderr)
+        return 1
+    missing = [c for c in ("portfolio", "core", "sp500") if not rosters.get(c)]
+    if missing:
+        print(f"ERROR: coverage rosters empty/missing: {', '.join(missing)}. The CM exports "
+              f"(portfolio.json/universe_metadata.json) + sigma-alert/sources/sp500.txt are "
+              f"REQUIRED for a correct local sync. Refusing to bake poisoned cohorts into "
+              f"watchlist.csv — check the sibling repos.", file=sys.stderr)
+        return 1
+    for r in new_rows:
+        r["cohort"] = cc.cohort_for(r["ticker"], rosters)
+    p, rr, co, sp = (rosters["portfolio"], rosters["researching"],
+                     rosters["core"], rosters["sp500"])
+    cohort_totals = {
+        "portfolio": len(p), "researching": len(rr - p),
+        "core": len(co - rr - p), "sp500": len(sp - co - rr - p),
+    }
+
+    by_subgroup: dict[str, int] = {}
+    for r in new_rows:
+        by_subgroup[r["sector_subgroup"]] = by_subgroup.get(r["sector_subgroup"], 0) + 1
+
+    print(f"Coverage source: {coverage_csv}")
+    print(f"Watchlist target: {WATCHLIST_CSV}")
+    print(f"Total in new watchlist: {len(new_rows)}")
+    for sg in sorted(by_subgroup):
+        print(f"  {sg}: {by_subgroup[sg]}")
+    foreign = sum(1 for r in new_rows if r["filer_type"] == "foreign")
+    print(f"Foreign (20-F/ADR -> Data Gap manual review): {foreign} of {len(new_rows)}")
+    n_cm = sum(1 for r in new_rows if r.get("source") == "cm")
+    n_sp = sum(1 for r in new_rows if r.get("source") == "sp500")
+    print(f"By source: cm={n_cm}  sp500={n_sp}")
+    print(f"Added (CM): {len(added)}")
+    print(f"Added (S&P 500 ring): {len(sp_added)}")
+    if sp_unresolved:
+        print(f"S&P 500 names skipped (no CIK in SEC map): {len(sp_unresolved)} "
+              f"-> {', '.join(sp_unresolved[:15])}{' ...' if len(sp_unresolved) > 15 else ''}")
+    print(f"Removed: {len(removed)}")
+    print(f"Subgroup changes: {len(subgroup_changes)}")
+
+    if added:
+        print("\n+ Added:")
+        for t in added[:50]:
+            print(f"  + {t}")
+        if len(added) > 50:
+            print(f"  ... and {len(added) - 50} more")
+    if removed:
+        print("\n- Removed:")
+        for t in removed[:50]:
+            print(f"  - {t}")
+        if len(removed) > 50:
+            print(f"  ... and {len(removed) - 50} more")
+    if subgroup_changes:
+        print("\n~ Subgroup changes:")
+        for t, old, new in subgroup_changes[:50]:
+            print(f"  ~ {t}: {old} -> {new}")
+
+    if args.dry_run:
+        print("\n[dry-run] no file written")
+        return 0
+
+    write_watchlist(new_rows)
+    print(f"\nWrote {len(new_rows)} rows to {WATCHLIST_CSV}")
+    if cohort_totals:
+        COHORT_TOTALS_JSON.write_text(json.dumps(cohort_totals, indent=2), encoding="utf-8")
+        print(f"Wrote cohort totals to {COHORT_TOTALS_JSON}: {cohort_totals}")
+    return 0
+
+
+if __name__ == "__main__":
+    raise SystemExit(main())

codex
I’m at the decision point now: the fixes address the four reported failure modes in the code path. The only execution limitation is test runner startup under this sandbox; it fails before collecting tests because there is no writable temp directory, not because of the code under review.
exec
/bin/bash -lc "nl -ba next_batch.py | sed -n '64,82p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
    64	                done.add(row["ticker"].strip())
    65	    return done
    66	
    67	
    68	def row_cohort(row: dict, rosters: dict[str, set[str]] | None = None) -> str:
    69	    """Baked `cohort` column (CI-safe) if present, else compute live from rosters.
    70	    `rosters=None` → `cohort_for` loads them live (defensive; avoids a hard dep on
    71	    the caller pre-loading rosters)."""
    72	    c = (row.get("cohort") or "").strip()
    73	    return c if c else cc.cohort_for(row.get("ticker", ""), rosters)
    74	
    75	
    76	def sort_key(row: dict, rosters: dict[str, set[str]] | None = None):
    77	    cohort_rank = cc.COHORT_RANK.get(row_cohort(row, rosters), 99)
    78	    sg_rank = SUBGROUP_ORDER.get(row.get("sector_subgroup", ""), 9)
    79	    return (cohort_rank, sg_rank, row.get("ticker", ""))
    80	
    81	
    82	def main() -> int:

exec
/bin/bash -lc "nl -ba run_unattended.py | sed -n '54,66p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
    54	
    55	def _pick_batch(n: int, cycle_start: str) -> list[str]:
    56	    """Next n un-screened domestic names (reuse next_batch's selection logic)."""
    57	    import next_batch as nb
    58	    watch = nb.load_watchlist()
    59	    rosters = nb.cc.load_rosters()  # empty in CI; sort_key falls back to the baked `cohort` column
    60	    domestic = [r for r in watch if r.get("filer_type", "domestic") != "foreign"]
    61	    done = nb.screened_since(cycle_start)
    62	    pending = [r for r in domestic if r["ticker"].strip() not in done]
    63	    pending.sort(key=lambda r: nb.sort_key(r, rosters))
    64	    return [r["ticker"].strip().upper() for r in pending[:n]]
    65	
    66	

exec
/bin/bash -lc "nl -ba sync_watchlist.py | sed -n '88,96p;268,290p;360,377p;430,435p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
    88	def ckey(t: str) -> str:
    89	    """Canonical ticker key for cross-source comparison. SEC / sigma-S&P / CM disagree
    90	    on class-share punctuation (`BRK.B` vs `BRK-B`, `BF.B` vs `BF-B`); fold `.`→`-` so
    91	    the same security matches across sources and isn't double-added. Distinct tickers
    92	    like GOOG vs GOOGL are unaffected (different letters, not punctuation)."""
    93	    return (t or "").strip().upper().replace(".", "-")
    94	
    95	
    96	def normalize_cik(raw: str) -> str:
   268	    core_set = rosters["core"]
   269	    # Compare on the canonical key so a class-ticker CM already carries (BRK-B) isn't
   270	    # re-added under sigma's spelling (BRK.B).
   271	    cm_canon = {ckey(t) for t in cm_master}
   272	    added_canon = {ckey(t) for t in already_added}
   273	    extras = sorted(t for t in rosters["sp500"]
   274	                    if ckey(t) not in cm_canon and ckey(t) not in added_canon)
   275	
   276	    sec_ciks = load_sec_ticker_ciks() if extras else {}
   277	    rows: list[dict] = []
   278	    added: list[str] = []
   279	    unresolved: list[str] = []
   280	    for ticker in extras:
   281	        prev = existing.get(ticker)
   282	        # Prefer a fresh SEC resolution, but fall back to the already-known CIK on an
   283	        # existing row so a transient SEC-map outage can't DROP established S&P rows
   284	        # (which would then be reported "removed" and written out of the watchlist).
   285	        cik = (sec_ciks.get(ticker) or sec_ciks.get(ticker.replace(".", "-"))
   286	               or (prev.get("cik") if prev and prev.get("cik") else ""))
   287	        if not cik:
   288	            unresolved.append(ticker)
   289	            continue  # no CIK -> can't EDGAR-screen; skip loudly (reported below)
   290	        rows.append({
   360	    # missing/empty, cohort_for would stamp everything "other"/"sp500" and bake zeros
   361	    # into cohort_totals.json — poisoned columns CI then trusts. So abort the write
   362	    # rather than persist a mis-partitioned universe.
   363	    cohort_totals: dict[str, int] = {}
   364	    try:
   365	        import coverage_cohorts as cc
   366	        rosters = cc.load_rosters()
   367	    except Exception as e:
   368	        print(f"ERROR: coverage_cohorts unavailable ({e}); cannot bake cohorts — refusing "
   369	              f"to write a watchlist without them.", file=sys.stderr)
   370	        return 1
   371	    missing = [c for c in ("portfolio", "core", "sp500") if not rosters.get(c)]
   372	    if missing:
   373	        print(f"ERROR: coverage rosters empty/missing: {', '.join(missing)}. The CM exports "
   374	              f"(portfolio.json/universe_metadata.json) + sigma-alert/sources/sp500.txt are "
   375	              f"REQUIRED for a correct local sync. Refusing to bake poisoned cohorts into "
   376	              f"watchlist.csv — check the sibling repos.", file=sys.stderr)
   377	        return 1
   430	    write_watchlist(new_rows)
   431	    print(f"\nWrote {len(new_rows)} rows to {WATCHLIST_CSV}")
   432	    if cohort_totals:
   433	        COHORT_TOTALS_JSON.write_text(json.dumps(cohort_totals, indent=2), encoding="utf-8")
   434	        print(f"Wrote cohort totals to {COHORT_TOTALS_JSON}: {cohort_totals}")
   435	    return 0

exec
/bin/bash -lc "nl -ba tests/test_sync_cohorts.py | sed -n '14,63p'" in /mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage
 succeeded in 0ms:
    14	def test_ckey_folds_class_punctuation():
    15	    # BRK.B / BRK-B are the same security across sources; GOOG vs GOOGL are not.
    16	    assert sw.ckey("BRK.B") == "BRK-B"
    17	    assert sw.ckey("brk.b") == "BRK-B"
    18	    assert sw.ckey("GOOGL") == "GOOGL"
    19	    assert sw.ckey("GOOG") != sw.ckey("GOOGL")
    20	
    21	
    22	def test_sort_key_defensive_without_rosters():
    23	    # Codex Critical: run_unattended called sort_key() with no rosters -> TypeError.
    24	    # sort_key must be callable without rosters (uses the baked cohort column).
    25	    k = nb.sort_key({"ticker": "AAA", "cohort": "portfolio"})
    26	    assert k[0] == cc.COHORT_RANK["portfolio"]
    27	    k2 = nb.sort_key({"ticker": "ZZZ", "cohort": "sp500"})
    28	    assert k2[0] == cc.COHORT_RANK["sp500"]
    29	    assert k < k2  # portfolio sorts before sp500
    30	
    31	
    32	def test_sp500_extras_preserves_cik_on_sec_failure(monkeypatch):
    33	    # Codex High: a transient SEC-map outage must NOT drop an existing S&P row
    34	    # (which would then be reported "removed" and written out of the watchlist).
    35	    monkeypatch.setattr(cc, "load_rosters", lambda: {
    36	        "portfolio": set(), "researching": set(), "core": set(), "sp500": {"ABNB"}})
    37	    monkeypatch.setattr(sw, "load_sec_ticker_ciks", lambda: {})  # SEC map unavailable
    38	    existing = {"ABNB": {"cik": "0001559720", "added_date": "2026-01-01",
    39	                         "company_name": "Airbnb"}}
    40	    rows, added, unresolved = sw.build_sp500_extras(
    41	        existing, cm_master=set(), already_added=set(), today="2026-07-13")
    42	    assert unresolved == []                       # NOT dropped
    43	    assert len(rows) == 1 and rows[0]["cik"] == "0001559720"
    44	
    45	
    46	def test_sp500_extras_dedup_class_ticker(monkeypatch):
    47	    # Codex High: CM carrying BRK-B must suppress re-adding sigma's BRK.B spelling.
    48	    monkeypatch.setattr(cc, "load_rosters", lambda: {
    49	        "portfolio": set(), "researching": set(), "core": set(), "sp500": {"BRK.B"}})
    50	    monkeypatch.setattr(sw, "load_sec_ticker_ciks", lambda: {"BRK-B": "0001067983"})
    51	    rows, added, unresolved = sw.build_sp500_extras(
    52	        {}, cm_master={"BRK-B"}, already_added=set(), today="2026-07-13")
    53	    assert rows == [] and added == []             # no duplicate row
    54	
    55	
    56	def test_sp500_extras_new_name_without_cik_is_skipped(monkeypatch):
    57	    # A brand-new S&P name with no prev row and no SEC CIK is skipped loudly (can't screen).
    58	    monkeypatch.setattr(cc, "load_rosters", lambda: {
    59	        "portfolio": set(), "researching": set(), "core": set(), "sp500": {"NEWCO"}})
    60	    monkeypatch.setattr(sw, "load_sec_ticker_ciks", lambda: {})
    61	    rows, added, unresolved = sw.build_sp500_extras(
    62	        {}, cm_master=set(), already_added=set(), today="2026-07-13")
    63	    assert rows == [] and unresolved == ["NEWCO"]

codex
**Round-1 Findings**
1. CRITICAL `run_unattended._pick_batch`: **FIXED** — it now passes rosters via lambda, and [row_cohort](</mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage/next_batch.py:72>) reads baked `cohort` before any roster fallback, so CI empty rosters still sort correctly.
2. HIGH SEC outage drops existing S&P rows: **FIXED** — [prev is assigned before CIK resolution](</mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage/sync_watchlist.py:280>) and `prev["cik"]` is used only for tickers still present in the current S&P roster, so it does not mask real removals.
3. HIGH class-ticker punctuation duplicate: **FIXED** — `ckey()` canonicalizes dot/dash and both `cm_master` and `already_added` are compared canonically before adding S&P extras.
4. HIGH poisoned empty cohorts: **FIXED** — sync returns `1` before writes when `coverage_cohorts` fails or `portfolio`/`core`/`sp500` are empty; `researching` is intentionally not checked, so a legitimately empty researching roster is not a false positive.

**New Critical/High**
No new Critical/High issues found.

The new regression tests assert the important behaviors. I could not run `pytest` in this sandbox because Python cannot create a temp file here, but direct import checks confirmed the key paths.
tokens used
90,823
**Round-1 Findings**
1. CRITICAL `run_unattended._pick_batch`: **FIXED** — it now passes rosters via lambda, and [row_cohort](</mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage/next_batch.py:72>) reads baked `cohort` before any roster fallback, so CI empty rosters still sort correctly.
2. HIGH SEC outage drops existing S&P rows: **FIXED** — [prev is assigned before CIK resolution](</mnt/c/Users/jroyp/Dropbox/Claude Folder/forensic_triage/sync_watchlist.py:280>) and `prev["cik"]` is used only for tickers still present in the current S&P roster, so it does not mask real removals.
3. HIGH class-ticker punctuation duplicate: **FIXED** — `ckey()` canonicalizes dot/dash and both `cm_master` and `already_added` are compared canonically before adding S&P extras.
4. HIGH poisoned empty cohorts: **FIXED** — sync returns `1` before writes when `coverage_cohorts` fails or `portfolio`/`core`/`sp500` are empty; `researching` is intentionally not checked, so a legitimately empty researching roster is not a false positive.

**New Critical/High**
No new Critical/High issues found.

The new regression tests assert the important behaviors. I could not run `pytest` in this sandbox because Python cannot create a temp file here, but direct import checks confirmed the key paths.
